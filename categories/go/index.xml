<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Go on 程式、雜談</title>
    <link>https://ronmi.github.io/categories/go/</link>
    <description>Recent content in Go on 程式、雜談</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>zh-tw</language>
    <lastBuildDate>Wed, 16 Sep 2015 14:26:42 +0800</lastBuildDate>
    <atom:link href="https://ronmi.github.io/categories/go/index.xml" rel="self" type="application/rss+xml" />
    
    <item>
      <title>有效地去 - Effective go 正體中文翻譯</title>
      <link>https://ronmi.github.io/post/go/effectivego/</link>
      <pubDate>Wed, 16 Sep 2015 14:26:42 +0800</pubDate>
      
      <guid>https://ronmi.github.io/post/go/effectivego/</guid>
      <description>

&lt;h2 id=&#34;簡介:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;簡介&lt;/h2&gt;

&lt;p&gt;Go 是一門新的程式語言。雖然它從現有的程式語言中借鏡了許多設計理念，但它也有許多與眾不同的特性。若你把用 C++ / Java 撰寫的程式改寫成等價的 Go 程式，恐怕難以得到令人滿意的結果。同樣地，用 Go 語言的角度來解決問題，你可能會寫出有效但不太一樣的程式。換句話說，想要寫出好的 Go 程式，對於 Go 語言特性及慣例的掌握是重要的一環。同時，對於社群慣例 (比如排版、命名方式、程式的建立等等) 也應該要適當的了解，這樣其他使用 Go 語言的開發者才容易理解你寫的程式。&lt;/p&gt;

&lt;p&gt;這份文件列舉出一些訣竅，讓你寫出清楚、符合 Go 語言特性的程式。這是對於 &lt;a href=&#34;https://golang.org/ref/spec&#34;&gt;Go 語言規格書&lt;/a&gt;、&lt;a href=&#34;https://tour.golang.org/&#34;&gt;導覽 Go 語言&lt;/a&gt; 以及 &lt;a href=&#34;https://golang.org/doc/code.html&#34;&gt;如何撰寫 Go 程式&lt;/a&gt; 等三份文件的補充，所以你應該先讀過這三份文件。&lt;/p&gt;

&lt;h3 id=&#34;範例:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;範例&lt;/h3&gt;

&lt;p&gt;&lt;a href=&#34;https://golang.org/src/&#34;&gt;Go 標準套件庫原始碼&lt;/a&gt; 除了作為核心程式庫之外，也是如何使用 Go 語言的範例。更進一步地，大部份的套件都會有一些可以直接在 &lt;a href=&#34;golang.org&#34;&gt;golang.org&lt;/a&gt; 網站上讓你實驗的範例，比如像是 &lt;a href=&#34;https://golang.org/pkg/strings/#example_Map&#34;&gt;這個&lt;/a&gt; (你也許會需要點擊 Example 字樣來顯示範例)。如果你對於如何呈現問題或是某事物的實作方式有疑問，Go 套件庫的文件、原始碼以及範例可以提供你靈感、解答或是相關的背景知識。&lt;/p&gt;

&lt;h2 id=&#34;排版格式:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;排版格式&lt;/h2&gt;

&lt;p&gt;排版風格問題是最常被討論但很少有結果的問題。人們可以套用各式各樣的風格，但如果大家的風格能統一的話會更好。風格統一最困難的一點，就是如何避免寫出落落長的風格規範。&lt;/p&gt;

&lt;p&gt;對於 Go 語言來說，我們使用了一種很少見的解決方式：讓機器來為我們處理排版問題。&lt;code&gt;gofmt&lt;/code&gt; 工具 (或是 &lt;code&gt;go fmt&lt;/code&gt;，它會將整個套件的原始碼都重新排版，而非只處理單一源碼檔案) 讀取 Go 程式碼，依照標準的排版風格把程式碼重新排版。如果你想弄清楚某種情況下應該怎麼排版，就執行 &lt;code&gt;gofmt&lt;/code&gt;。如果結果看起來怪怪的，你可以調整你的程式 (或是提出 &lt;code&gt;gofmt&lt;/code&gt; 的錯誤回報)，千萬不要削足適履 。&lt;/p&gt;

&lt;p&gt;舉個例子，你再也不需要拼命按 Tab 和空白鍵去對齊你的行內註解：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type T struct {
    name string // name of the object
    value int // its value
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;code&gt;gofmt&lt;/code&gt; 會幫你處理好&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type T struct {
    name    string // name of the object
    value   int    // its value
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;標準套件庫的原始碼都有使用 &lt;code&gt;gofmt&lt;/code&gt; 排版過。&lt;/p&gt;

&lt;p&gt;當然還有些排版的細節我們沒有提到，簡略的說：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;縮排：我們使用 Tab 來縮排，&lt;code&gt;gofmt&lt;/code&gt; 預設會忽略它們。非必要的話別使用空白來縮排。&lt;br /&gt;&lt;/li&gt;
&lt;li&gt;行寬：Go 沒有行寬限制，不用擔心你的程式碼太長。如果你真的覺得它太長了，可以換行並縮排它。&lt;br /&gt;&lt;/li&gt;
&lt;li&gt;括號：Go 需要的括號遠比 C++ / Java 來得少：流程控制敘述 (if, for, switch) 沒有小括號。運算子之間的優先順序也會以排版的方式突顯。比如&lt;br /&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;x&amp;lt;&amp;lt;8 + y&amp;lt;&amp;lt;16
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;註解:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;註解&lt;/h2&gt;

&lt;p&gt;Go 提供的 C 風格的區塊註解 /* */ 以及 C++ 風格的行內註解 //。行內註解比較常用到，區塊註解通常是用來說明整個套件，但當你想要暫時停用某段程式碼的時候也很適合。&lt;/p&gt;

&lt;p&gt;&lt;code&gt;godoc&lt;/code&gt; 程式 (它同時也是一個 web server) 會解析原始碼中的註解。在最外層的宣告語句上面的註解，如果沒有被空行隔開的話，會當成是那個語句的說明。這些說明的內容決定了 &lt;code&gt;godoc&lt;/code&gt; 是否能產生出高品質的說明文件。&lt;/p&gt;

&lt;p&gt;每個套件都應該要有一份套件說明，也就是在 package 語法上方的區塊註解。如果套件裡有多個源碼檔案，註解可以寫在其中任何一個檔案裡。套件說明應該是整體性的說明，或是提供整個套件的通用資訊。套件說明在 &lt;code&gt;godoc&lt;/code&gt; 產生出來的文件裡會在頁面的最上方，所以你應該寫詳細點，像是這樣：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;/*
Package regexp implements a simple library for regular expressions.

The syntax of the regular expressions accepted is:

    regexp:
        concatenation { &#39;|&#39; concatenation }
    concatenation:
        { closure }
    closure:
        term [ &#39;*&#39; | &#39;+&#39; | &#39;?&#39; ]
    term:
        &#39;^&#39;
        &#39;$&#39;
        &#39;.&#39;
        character
        &#39;[&#39; [ &#39;^&#39; ] character-ranges &#39;]&#39;
        &#39;(&#39; regexp &#39;)&#39;
*/
package regexp
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如果套件很簡單，說明當然也可以簡短些：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// Package path implements utility routines for
// manipulating slash-separated filename paths.
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;註解不需要用星號一類的字元做出框框，因為產生出來的文件不一定會用固定寬度的文字來呈現，所以不要用空白一類的方式來排版，&lt;code&gt;godoc&lt;/code&gt; 會幫你處理排版問題。註解應該是純文字，避免使用像 HTML 或是其他標記方式。&lt;code&gt;godoc&lt;/code&gt; 的其中一種排版方式，是把縮排後的文字用固定寬度文字顯示，方便你在說明中嵌入範例程式碼。&lt;a href=&#34;https://golang.org/pkg/fmt/&#34;&gt;fmt 套件&lt;/a&gt; 的說明是個好例子。&lt;/p&gt;

&lt;p&gt;某些情況下 &lt;code&gt;godoc&lt;/code&gt; 也許完全不會重新排版，所以你應該讓你的註解容易閱讀：不要寫錯別字、正確使用標點符號、在適當的地方斷句等等。&lt;/p&gt;

&lt;p&gt;套件裡每個最外層的宣告語句，其上方的註解都會被當成是說明文件。每個公開的識別名稱 (exported，就是首字大寫的名稱) 都應該要有一份說明。&lt;/p&gt;

&lt;p&gt;說明文件應該要是完整的句子，第一句應該是簡略的說明，並且以宣告語句所宣告的識別名稱做為開頭。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// Compile parses a regular expression and returns, if successful, a Regexp
// object that can be used to match against text.
func Compile(str string) (regexp *Regexp, err error) {
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這樣的設計是為了方便你使用 &lt;code&gt;grep&lt;/code&gt; 來搜尋用 &lt;code&gt;godoc&lt;/code&gt; 產生出來的文件。假設你忘記 regexp 套件中某個函式的名稱，但你記得這個函式會分析 (parse) 正則式，你可能會這樣子搜尋文件：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-sh&#34;&gt;godoc regexp | grep parse
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如果你的說明文件寫「這個函式是用來&amp;hellip;」，顯然你不會因此想起函式名稱來。但如果你把名稱寫在說明文件的開頭，那你就會知道可能是 Compile 函式：&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;$ godoc regexp | grep parse
    Compile parses a regular expression and returns, if successful, a Regexp
    parsed. It simplifies safe initialization of global variables holding
    cannot be parsed. It simplifies safe initialization of global variables
$
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Go 的宣告語法允許把同質性的宣告包成一個群組，所以你可以在群組上方加一段註解來進行整體性的說明。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// Error codes returned by failures to parse an expression.
var (
    ErrInternal      = errors.New(&amp;quot;regexp: internal error&amp;quot;)
    ErrUnmatchedLpar = errors.New(&amp;quot;regexp: unmatched &#39;(&#39;&amp;quot;)
    ErrUnmatchedRpar = errors.New(&amp;quot;regexp: unmatched &#39;)&#39;&amp;quot;)
    ...
)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;群組宣告也可以用來表示變數之間有某種關聯，比如某些被同步鎖保護的變數：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;var (
    countLock   sync.Mutex
    inputCount  uint32
    outputCount uint32
    errorCount  uint32
)
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;命名規則:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;命名規則&lt;/h2&gt;

&lt;p&gt;就像在其他語言一樣，命名規則對 Go 語言來說是十分重要的。更進一步地，它還有語意上的效果：一個變數、常數、函式是否能被其他套件的程式碼取用，端看它的名稱是否是以大寫字母開頭。所以命名規則實在值得我們花點篇幅好好介紹一番。&lt;/p&gt;

&lt;h3 id=&#34;套件命名:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;套件命名&lt;/h3&gt;

&lt;p&gt;當你引用 (import) 某個套件之後，它的名稱便成為你存取相關資源的憑據。比如你&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;import &amp;quot;bytes&amp;quot;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;之後，就可以用 &lt;code&gt;bytes.Buffer&lt;/code&gt; 來存取 bytes 套件中定義的 Buffer。如果每個人都可以用相同的名字來存取相同的套件內容，事情就會簡單許多；這代表了套件的名字要取的簡潔有力。慣例上，套件名稱應該是小寫的單字，避免使用底線或大小寫混雜。考慮到大家都會在程式碼中一再輸入各種名稱，像是 &lt;em&gt;Err&lt;/em&gt; 這樣夠簡短，又能讓人一眼看出來的名稱會更好。套件名稱只是引用時的預設名稱，所以不用擔心會和其他套件撞名，也不需要強制在每個源碼檔中使用相同的名稱。如果真的發生撞名的情況，你完全可以在引用的時候指定一個別名給它。通常你不會因此搞混，因為引用時的完整套件名稱讓你可以確定自己是引用了什麼。&lt;/p&gt;

&lt;p&gt;另一個慣例是我們會用原始碼的目錄作為套件名稱：放在 &lt;em&gt;src/encoding/base64&lt;/em&gt; 目錄裡的套件會以 &lt;em&gt;encoding/base64&lt;/em&gt; 這個名稱引入，不是 &lt;em&gt;encoding_base64&lt;/em&gt; 也不是 *encodingBase64*。&lt;/p&gt;

&lt;p&gt;程式可以用套件名稱來參考套件中公開的資源，這樣可以避免混淆。切勿使用 &lt;code&gt;.&lt;/code&gt; 來引入，這是設計來簡化一些必須在套件外進行的測試，除此之外的用途應當盡量避免。例如在 &lt;code&gt;bufio&lt;/code&gt; 套件中，具有緩衝區的可讀取串流會命名為 &lt;code&gt;Reader&lt;/code&gt; 而不是 &lt;code&gt;BufReader&lt;/code&gt;，因為 &lt;code&gt;bufio.Reader&lt;/code&gt; 比 &lt;code&gt;bufio.BufReader&lt;/code&gt; 更簡短也更精確。進一步來說，因為引用的時候會以套件名稱作為前綴，所以 &lt;code&gt;bufio.Reader&lt;/code&gt; 不會跟 &lt;code&gt;io.Reader&lt;/code&gt; 混淆。同樣的，用來產生一個新的 &lt;code&gt;ring.Ring&lt;/code&gt; 實體的函式 (在 Go 語言裡稱為建構子)，通常會命名為 &lt;code&gt;NewRing&lt;/code&gt;，但考慮到 &lt;code&gt;ring&lt;/code&gt; 套件裡只有 &lt;code&gt;Ring&lt;/code&gt; 這個公開的 struct，而且套件名稱跟 struct 名稱又重複，所以取名為 &lt;code&gt;New&lt;/code&gt; 可以更加簡潔：其他人會使用 &lt;code&gt;ring.New&lt;/code&gt; 來呼叫它。總之就是利用套件結構來幫助你選擇一個好名字。&lt;/p&gt;

&lt;p&gt;另一個好例子是 &lt;code&gt;once.Do&lt;/code&gt;：寫 &lt;code&gt;once.Do(setup)&lt;/code&gt; 很清楚，就算你寫的更長 &lt;code&gt;once.DoOrWaitUntilDone(setup)&lt;/code&gt; 也不會更清楚。很長的名稱不見得讓人容易理解，清楚又精確的說明文件會更有效果。&lt;/p&gt;

&lt;h3 id=&#34;成員存取方法-getter-setter:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;成員存取方法 (Getter / Setter)&lt;/h3&gt;

&lt;p&gt;Go 語言並沒有在語言層面支援成員存取方法。使用成員存取方法沒有錯，而且通常是不錯的設計模式，但是在成員存取方法前加上 Get 或 Set 既不必要，也不符合 Go 語言的慣例。如果你有個成員變數叫做 &lt;code&gt;owner&lt;/code&gt; (小寫字母開頭代表不能公開存取)，那麼相關的成員存取方法應該稱為 &lt;code&gt;Owner()&lt;/code&gt; (大寫字母開頭) 以及 &lt;code&gt;SetOwner()&lt;/code&gt;。這樣閱讀起來也十分清楚：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;owner := obj.Owner()
if owner != user {
    obj.SetOwner(user)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;介面名稱:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;介面名稱&lt;/h3&gt;

&lt;p&gt;慣例上，只有一個方法的介面，會用方法的名字加上 er 字尾，或是其他類似的方式來命名，這樣可以營造一種代理人的感覺，像是 &lt;code&gt;Reader&lt;/code&gt;, &lt;code&gt;Writer&lt;/code&gt;, &lt;code&gt;Formatter&lt;/code&gt; 等等。&lt;/p&gt;

&lt;p&gt;像這樣的名字很多，而遵守這個慣例會讓你更有生產力。像是 &lt;code&gt;Read&lt;/code&gt;, &lt;code&gt;Write&lt;/code&gt;, &lt;code&gt;Close&lt;/code&gt;, &lt;code&gt;String&lt;/code&gt; 這些都是，它們都有標準化的定義。為了避免混淆，除非你寫的函式和它們有相同的定義，否則就不要使用這些名字。相反地，如果你實做了相同的定義的函式，那麼就應該照它們的方式命名，比如用 &lt;code&gt;String&lt;/code&gt; 而不是 &lt;code&gt;ToString&lt;/code&gt;。&lt;/p&gt;

&lt;h3 id=&#34;大小寫混合:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;大小寫混合&lt;/h3&gt;

&lt;p&gt;最後，如果你需要使用數個單字來做命名，用 &lt;code&gt;MixedCaps&lt;/code&gt; 或是 &lt;code&gt;mixedCaps&lt;/code&gt;，不要用底線。&lt;/p&gt;

&lt;h2 id=&#34;分號:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;分號&lt;/h2&gt;

&lt;p&gt;Go 也像 C 語言一樣使用分號來分隔敘述，但你在源碼檔中看不見它們。Go 的編譯器 (正確來說是語法分析器 lexer) 會依照簡單的規則自動在分析語法的時候幫你加上分號，所以通常你不需要自己動手。&lt;/p&gt;

&lt;p&gt;規則很單純，如果一行的最後一個語法單元是識別子 (identifier)、簡單表達式 (像是數字或字串常數)、或是以下幾個關鍵字之一&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;break continue fallthrough return ++ -- ) }
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;編譯器會在這些語法單元後面加上分號。簡單來說，如果某一行是以「代表語句終結」的關鍵字結束，就加上分號。&lt;/p&gt;

&lt;p&gt;在右大括號前面的分號也可以省略：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;go func() { for { dst &amp;lt;- &amp;lt;-src } }()
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Go 程式通常只會在 &lt;code&gt;for&lt;/code&gt; 敘述裡使用分號，這是用來隔開迴圈的初始、條件、後處理這三個寫在同一行的敘述。&lt;/p&gt;

&lt;p&gt;這個自動加分號的規則暗示了你不可以把流程控制語句 (&lt;code&gt;for&lt;/code&gt;, &lt;code&gt;if&lt;/code&gt;, &lt;code&gt;switch&lt;/code&gt;, &lt;code&gt;select&lt;/code&gt;) 的左大括號放到下一行，因為分號會加到大括號的前面去，而顯然這不會是你想要的結果。所以應該要寫成這樣&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;if i &amp;lt; f() {
    g()
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;流程控制:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;流程控制&lt;/h2&gt;

&lt;p&gt;Go 的流程控制語法跟 C 很像，但有很多重要的不同。Go 沒有 &lt;code&gt;do&lt;/code&gt; 和 &lt;code&gt;while&lt;/code&gt;，只有比 C 更通用一點的 &lt;code&gt;for&lt;/code&gt;；&lt;code&gt;switch&lt;/code&gt; 比較有彈性；&lt;code&gt;if&lt;/code&gt; 跟 &lt;code&gt;switch&lt;/code&gt; 可以在條件判斷式之前加上一個初始語句，有點像 &lt;code&gt;for&lt;/code&gt; 那樣；&lt;code&gt;break&lt;/code&gt; 和 &lt;code&gt;continue&lt;/code&gt; 可以指定標記；而且還有一些新的特性：支援型別判斷的 &lt;code&gt;switch&lt;/code&gt; 和頻道多工器 &lt;code&gt;select&lt;/code&gt;。語法上也有些微不同：條件式不用加小括號、不能省略大括號。&lt;/p&gt;

&lt;h3 id=&#34;if:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;if&lt;/h3&gt;

&lt;p&gt;簡單的 &lt;code&gt;if&lt;/code&gt; 語句像是這樣&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;if x &amp;gt; 0 {
    return y
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;不能省略的大括號暗示了應該避免寫單行的 &lt;code&gt;if&lt;/code&gt; 語句，尤其當裡面有 &lt;code&gt;return&lt;/code&gt; 或 &lt;code&gt;break&lt;/code&gt; 的時候更是如此。&lt;/p&gt;

&lt;p&gt;因為 &lt;code&gt;if&lt;/code&gt; 像 &lt;code&gt;for&lt;/code&gt; 一樣可以接受初始語句，所以在 &lt;code&gt;if&lt;/code&gt; 語句初始一個區域變數是很常見的寫法：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;if err := file.Chmod(0664); err != nil {
    log.Print(err)
    return err
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;在 Go 的核心程式庫裡，你會發現很多可能不會執行下一行程式的 &lt;code&gt;if&lt;/code&gt; 語句 (比如說裡面的最後一個敘述是 &lt;code&gt;return&lt;/code&gt;, &lt;code&gt;break&lt;/code&gt; continue&lt;code&gt;之類的) 都省略掉了&lt;/code&gt;else` 敘述：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;f, err := os.Open(name)
if err != nil {
    return err
}
codeUsing(f)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這是一般常見用來偵測、防止例外情況的寫法。如果你的程式一路上把出錯的情況都挑掉，正常的情況執行下去，那麼它就會很容易閱讀。如果錯誤都透過 &lt;code&gt;return&lt;/code&gt; 敘述離開你的函式，那麼最後寫出來的程式碼就不會有 &lt;code&gt;else&lt;/code&gt; 敘述：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;f, err := os.Open(name)
if err != nil {
    return err
}
d, err := f.Stat()
if err != nil {
    f.Close()
    return err
}
codeUsing(f, d)
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;重新宣告與重新指定:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;重新宣告與重新指定&lt;/h3&gt;

&lt;p&gt;上一節的最後一個範例展示了短式宣告 &lt;code&gt;:=&lt;/code&gt; 的使用方式。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;f, err := os.Open(name)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這一行宣告了 &lt;code&gt;f&lt;/code&gt; 和 &lt;code&gt;err&lt;/code&gt; 兩個變數。而後面的&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;d, err := f.Stat()
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;看起來好像是宣告了 &lt;code&gt;d&lt;/code&gt; 和 &lt;code&gt;err&lt;/code&gt; 兩個變數。要注意的是，雖然看起來 &lt;code&gt;err&lt;/code&gt; 好像宣告了兩次，但這是合法的敘述：&lt;code&gt;err&lt;/code&gt; 在第一個敘述中宣告，而第二個敘述裡只是重新指定了新的值。也就是說在呼叫 &lt;code&gt;f.Stat&lt;/code&gt; 的時候，&lt;code&gt;err&lt;/code&gt; 會使用之前宣告的那個變數，只是賦予新的值給它。&lt;/p&gt;

&lt;p&gt;所以若是滿足以下三個條件，使用 &lt;code&gt;:=&lt;/code&gt; 宣告先前宣告過的變數會變成賦予新值：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;必須要在相同的有效範圍 (scope) 內。如果是不同的有效範圍就會變成宣告一個同名的、全新的區域變數 (註)。&lt;br /&gt;&lt;/li&gt;
&lt;li&gt;型別要相同。&lt;br /&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;:=&lt;/code&gt; 左邊至少要有一個之前沒有宣告過的變數。&lt;br /&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;這個特性雖然不太常見，但卻很實用。這會讓你簡單地重用相同的 &lt;code&gt;err&lt;/code&gt; 變數，這種用法在一長串的 &lt;em&gt;if-else&lt;/em&gt; 中特別常見。&lt;/p&gt;

&lt;p&gt;備註：值得注意的是，在 Go 語言中，函式的參數及回傳值雖然寫在函式大括號的外面，但其實它們跟函式本體屬於同一個有效範圍。&lt;/p&gt;

&lt;h3 id=&#34;for:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;for&lt;/h3&gt;

&lt;p&gt;Go 裡面的 &lt;code&gt;for&lt;/code&gt; 跟 C 裡面的有點像又不太像。它把 &lt;code&gt;for&lt;/code&gt; 跟 &lt;code&gt;while&lt;/code&gt; 整合在一起：因為 Go 沒有 *do-while*。&lt;code&gt;for&lt;/code&gt; 有三種形式：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// Like a C for
for init; condition; post { }

// Like a C while
for condition { }

// Like a C for(;;)
for { }
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;短式宣告讓你可以輕易宣告一個只在迴圈中有效的變數：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;sum := 0
for i := 0; i &amp;lt; 10; i++ {
    sum += i
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如果你要遍歷一個 array, slice, string 或是 map，你可以使用 &lt;code&gt;range&lt;/code&gt; 語法：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;for key, value := range oldMap {
    newMap[key] = value
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如果只要 key 值的話：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;for key := range m {
    if key.expired() {
        delete(m, key)
    }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;相反地，不要 key 值的話：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;sum := 0
for _, value := range array {
    sum += value
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;空識別子 (&lt;code&gt;_&lt;/code&gt;) 有很多用法，會在稍後的章節介紹。&lt;/p&gt;

&lt;p&gt;在遍歷字串 (string) 的時候，&lt;code&gt;for&lt;/code&gt; 會幫你做些處理：幫你解析 UTF8，斷字在正確的位元。錯誤的編碼會消耗一個位元組 (byte)，並回傳一個特別的 &lt;code&gt;rune&lt;/code&gt; (&lt;code&gt;rune&lt;/code&gt; 是 Go 的術語，指的是一個 UTF8 的字元。在 Go 裡有套件專門處理 &lt;code&gt;rune&lt;/code&gt;) U+FFFD。以下的程式碼：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;for pos, char := range &amp;quot;日本\x80語&amp;quot; { // \x80 is an illegal UTF-8 encoding
    fmt.Printf(&amp;quot;character %#U starts at byte position %d\n&amp;quot;, char, pos)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;會顯示&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;character U+65E5 &#39;日&#39; starts at byte position 0
character U+672C &#39;本&#39; starts at byte position 3
character U+FFFD &#39;�&#39; starts at byte position 6
character U+8A9E &#39;語&#39; starts at byte position 7
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;最後，Go 沒有逗號這個運算子，而且 &lt;code&gt;++&lt;/code&gt; 和 &lt;code&gt;--&lt;/code&gt; 是完整的語句而不是敘述的一部份。如果你需要在 &lt;code&gt;for&lt;/code&gt; 裡使用多個變數，你可以用多重指定：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// Reverse a
for i, j := 0, len(a)-1; i &amp;lt; j; i, j = i+1, j-1 {
    a[i], a[j] = a[j], a[i]
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;switch:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;switch&lt;/h3&gt;

&lt;p&gt;Go 的 &lt;code&gt;switch&lt;/code&gt; 比 C 的要泛用。&lt;code&gt;case&lt;/code&gt; 敘述比對的值不一定要是常數。&lt;code&gt;case&lt;/code&gt; 敘述會依上到下的順序一一比對，直到找到符合的項目。如果 &lt;code&gt;switch&lt;/code&gt; 後面沒有東西，那麼就會當成是 &lt;code&gt;switch true&lt;/code&gt;。所以你可以 (而且這也是慣例) 把一長串的 &lt;code&gt;if-else if&lt;/code&gt; 改寫成 &lt;code&gt;switch&lt;/code&gt;：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func unhex(c byte) byte {
    switch {
    case &#39;0&#39; &amp;lt;= c &amp;amp;&amp;amp; c &amp;lt;= &#39;9&#39;:
        return c - &#39;0&#39;
    case &#39;a&#39; &amp;lt;= c &amp;amp;&amp;amp; c &amp;lt;= &#39;f&#39;:
        return c - &#39;a&#39; + 10
    case &#39;A&#39; &amp;lt;= c &amp;amp;&amp;amp; c &amp;lt;= &#39;F&#39;:
        return c - &#39;A&#39; + 10
    }
    return 0
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;不像 C 語言，Go 的 &lt;code&gt;switch&lt;/code&gt; 不會跨越到下一個 &lt;code&gt;case&lt;/code&gt; 執行，但你可以使用逗號代表「這些值都可以」：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func shouldEscape(c byte) bool {
    switch c {
    case &#39; &#39;, &#39;?&#39;, &#39;&amp;amp;&#39;, &#39;=&#39;, &#39;#&#39;, &#39;+&#39;, &#39;%&#39;:
        return true
    }
    return false
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;雖然在 Go 裡不常用，但你確實可以用 &lt;code&gt;break&lt;/code&gt; 跳出 &lt;code&gt;switch&lt;/code&gt;，就像在其他類 C 的語言裡一樣。有時候你不止想要跳出 &lt;code&gt;switch&lt;/code&gt;，可能想要連外層的 &lt;code&gt;for&lt;/code&gt; 迴圈一併跳過，你可以用 &lt;code&gt;break&lt;/code&gt; 加標記的方式達成：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;Loop:
	for n := 0; n &amp;lt; len(src); n += size {
		switch {
		case src[n] &amp;lt; sizeOne:
			if validateOnly {
				break
			}
			size = 1
			update(src[n])

		case src[n] &amp;lt; sizeTwo:
			if n+1 &amp;gt;= len(src) {
				err = errShortInput
				break Loop
			}
			if validateOnly {
				break
			}
			size = 2
			update(src[n] + src[n+1]&amp;lt;&amp;lt;shift)
		}
	}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;當然，&lt;code&gt;continue&lt;/code&gt; 也可以加標記，但只能用在迴圈上。&lt;/p&gt;

&lt;p&gt;讓我們用一個例子來結束這一節：比對兩個 []byte 是否具有相同的內容&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// Compare returns an integer comparing the two byte slices,
// lexicographically.
// The result will be 0 if a == b, -1 if a &amp;lt; b, and +1 if a &amp;gt; b
func Compare(a, b []byte) int {
    for i := 0; i &amp;lt; len(a) &amp;amp;&amp;amp; i &amp;lt; len(b); i++ {
        switch {
        case a[i] &amp;gt; b[i]:
            return 1
        case a[i] &amp;lt; b[i]:
            return -1
        }
    }
    switch {
    case len(a) &amp;gt; len(b):
        return 1
    case len(a) &amp;lt; len(b):
        return -1
    }
    return 0
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;判斷型別的-switch-type-switch:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;判斷型別的 switch (type switch)&lt;/h3&gt;

&lt;p&gt;&lt;code&gt;switch&lt;/code&gt; 也可以用來判斷動態型別，這會用到型別斷言 (type assertion) 的語法及 &lt;code&gt;type&lt;/code&gt; 關鍵字。如果你宣告一個區域變數給它，那麼這個區域變數的型別會和 &lt;code&gt;case&lt;/code&gt; 敘述比對的型別相同：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;var t interface{}
t = functionOfSomeType()
switch t := t.(type) {
default:
    fmt.Printf(&amp;quot;unexpected type %T\n&amp;quot;, t)     // %T prints whatever type t has
case bool:
    fmt.Printf(&amp;quot;boolean %t\n&amp;quot;, t)             // t has type bool
case int:
    fmt.Printf(&amp;quot;integer %d\n&amp;quot;, t)             // t has type int
case *bool:
    fmt.Printf(&amp;quot;pointer to boolean %t\n&amp;quot;, *t) // t has type *bool
case *int:
    fmt.Printf(&amp;quot;pointer to integer %d\n&amp;quot;, *t) // t has type *int
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;函式:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;函式&lt;/h2&gt;

&lt;h3 id=&#34;多重傳回值:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;多重傳回值&lt;/h3&gt;

&lt;p&gt;Go 有個很不尋常的特性：函式及方法可以回傳多個值。這個特性可以改善一些 C 語言的不良慣例：出錯時回傳負值，並改變某個傳址的參數。&lt;/p&gt;

&lt;p&gt;在 C 語言裡，寫入錯誤會回傳 -1 (寫入了 -1 個位元組，顯然你知道這有問題)，然後把錯誤碼寫進某個傳址進來的參數。在 Go 語言裡，我們會回傳寫入了多少位元組 (這點跟 C 差不多)，同時也回傳錯誤碼：「好，你確實寫了幾 byte 進去，不過不是全部，因為磁碟機滿了」。所以 Go 語言裡的 &lt;code&gt;Write&lt;/code&gt; 函式的定義是這樣的：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func (file *File) Write(b []byte) (n int, err error)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如同說明文件所示，它會回傳已經寫入的位元組數，如果這與 &lt;code&gt;b&lt;/code&gt; 的長度不符，err 就會是錯誤碼。你可以在稍後 &lt;em&gt;錯誤處理&lt;/em&gt; 的章節看到更多例子。&lt;/p&gt;

&lt;p&gt;這樣的形式消除了回傳參考的需求：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func nextInt(b []byte, i int) (int, int) {
    for ; i &amp;lt; len(b) &amp;amp;&amp;amp; !isDigit(b[i]); i++ {
    }
    x := 0
    for ; i &amp;lt; len(b) &amp;amp;&amp;amp; isDigit(b[i]); i++ {
        x = x*10 + int(b[i]) - &#39;0&#39;
    }
    return x, i
}

for i := 0; i &amp;lt; len(b); {
    x, i = nextInt(b, i)
    fmt.Println(x)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;回傳值的預先命名:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;回傳值的預先命名&lt;/h3&gt;

&lt;p&gt;回傳值可以預先給予名字，這樣就可以在函式內像一般變數那樣地使用它。它們會被初始化為零值，而遇到沒有加上回傳值的 &lt;code&gt;return&lt;/code&gt; 語句時，就會把它們傳回去。&lt;/p&gt;

&lt;p&gt;這種寫法不是預設的，但很多時候可以讓你的程式碼清楚不少：這等於在寫說明文件。如果我們把剛剛的 &lt;code&gt;nextInt&lt;/code&gt; 函式的回傳值加上命名，你就能一目瞭然地理解哪個 int 是什麼意義：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func nextInt(b []byte, pos int) (value, nextPos int) {
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;因為這些變數會初始化為零值，如果用的好的話可以讓整段程式都簡化許多。以下以 &lt;code&gt;io.ReadFull&lt;/code&gt; 做為例子：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func ReadFull(r Reader, buf []byte) (n int, err error) {
    for len(buf) &amp;gt; 0 &amp;amp;&amp;amp; err == nil {
        var nr int
        nr, err = r.Read(buf)
        n += nr
        buf = buf[nr:]
    }
    return
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;defer:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;Defer&lt;/h3&gt;

&lt;p&gt;&lt;code&gt;defer&lt;/code&gt; 語句是把某個函式的呼叫排程在離開目前函式時才執行。這個特性不是很常用，但在某些情況下，比如釋放的資源，會非常有用。典型的例子是釋放同步鎖或是關閉檔案：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// Contents returns the file&#39;s contents as a string.
func Contents(filename string) (string, error) {
    f, err := os.Open(filename)
    if err != nil {
        return &amp;quot;&amp;quot;, err
    }
    defer f.Close()  // f.Close will run when we&#39;re finished.

    var result []byte
    buf := make([]byte, 100)
    for {
        n, err := f.Read(buf[0:])
        result = append(result, buf[0:n]...) // append is discussed later.
        if err != nil {
            if err == io.EOF {
                break
            }
            return &amp;quot;&amp;quot;, err  // f will be closed if we return here.
        }
    }
    return string(result), nil // f will be closed if we return here.
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;使用 &lt;code&gt;defer&lt;/code&gt; 有兩大好處：首先，你不會因為忘記釋放資源或關閉檔案造成程式錯誤；其次，如果你在開啟檔案之後馬上用 &lt;code&gt;defer&lt;/code&gt; 語句來預約關閉檔案，會比在程式結尾關閉檔案更好閱讀。&lt;/p&gt;

&lt;p&gt;使用 &lt;code&gt;defer&lt;/code&gt; 時，參數會在你寫下 &lt;code&gt;defer&lt;/code&gt; 本身執行的時候就確定它的值，而不是在函式真正執行的時候。所以不用擔心後續的變數操作，同時也表示你可以用一行 &lt;code&gt;defer&lt;/code&gt; 來定義數個操作，以下是個不太優雅的例子：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;for i := 0; i &amp;lt; 5; i++ {
    defer fmt.Printf(&amp;quot;%d &amp;quot;, i)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;code&gt;defer&lt;/code&gt; 會以相反的順序執行，也就是最先定義的 &lt;code&gt;defer&lt;/code&gt; 會最後執行。所以上面的範例最後會印出「4 3 2 1 0」。有個比較可能的例子：用來追蹤函式的執行：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func trace(s string)   { fmt.Println(&amp;quot;entering:&amp;quot;, s) }
func untrace(s string) { fmt.Println(&amp;quot;leaving:&amp;quot;, s) }

// Use them like this:
func a() {
    trace(&amp;quot;a&amp;quot;)
    defer untrace(&amp;quot;a&amp;quot;)
    // do something....
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;由於在 &lt;code&gt;defer&lt;/code&gt; 執行的時候，參數的值便已經確定，我們可以好好利用這個特性：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func trace(s string) string {
    fmt.Println(&amp;quot;entering:&amp;quot;, s)
    return s
}

func un(s string) {
    fmt.Println(&amp;quot;leaving:&amp;quot;, s)
}

func a() {
    defer un(trace(&amp;quot;a&amp;quot;))
    fmt.Println(&amp;quot;in a&amp;quot;)
}

func b() {
    defer un(trace(&amp;quot;b&amp;quot;))
    fmt.Println(&amp;quot;in b&amp;quot;)
    a()
}

func main() {
    b()
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這會印出&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;entering: b
in b
entering: a
in a
leaving: a
leaving: b
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;對於已經習慣以程式區塊作為資源管理單位的開發者，這樣的行為可能會有點詭異，不過事實上 &lt;code&gt;defer&lt;/code&gt; 的資源管理單位是函式，這也是它強大之處。後續在 &lt;em&gt;panic 和 recover&lt;/em&gt; 的章節我們會看到 &lt;code&gt;defer&lt;/code&gt; 的其他可能性。&lt;/p&gt;

&lt;p&gt;譯註：上一段翻譯品質不佳，需要 PR&lt;/p&gt;

&lt;h2 id=&#34;資料:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;資料&lt;/h2&gt;

&lt;h3 id=&#34;用-new-來配置:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;用 &lt;em&gt;new&lt;/em&gt; 來配置&lt;/h3&gt;

&lt;p&gt;Go 有兩個配置資源的內建函式：&lt;code&gt;new&lt;/code&gt; 和 &lt;code&gt;make&lt;/code&gt;。兩者適用於不同的場合，也許看起來有點讓人容易搞混，但其實很簡單。我們先來看 &lt;code&gt;new&lt;/code&gt;。雖然它確實會配置記憶體，但不像在其他語言那樣，Go 的 new 函式不會「建構」它 (譯註)，只會填入零值。也就是說，&lt;code&gt;new(T)&lt;/code&gt; 配置了一塊記憶體，填入符合 &lt;code&gt;T&lt;/code&gt; 型別的零值，然後把位址傳回來。用 Go 的講法，就是回傳一個 &lt;code&gt;T&lt;/code&gt; 型別的指標，記憶體內容是零值。&lt;/p&gt;

&lt;p&gt;譯註：原文 &lt;em&gt;but unlike its namesakes in some other languages it does not initialize the memory&lt;/em&gt; 中的 &lt;em&gt;initialize&lt;/em&gt; 通常翻成「初始化」，但此處語意應該是「不像 C++ 或 Java 那樣會幫你呼叫建構子 (constructor) *處理成員的初始值*」，所以改譯為「建構」。&lt;/p&gt;

&lt;p&gt;既然 &lt;code&gt;new&lt;/code&gt; 會為你填入零值，你可以把你的資料結構設計成零值就能用，那麼寫起程式來就會方便很多。比如像 &lt;code&gt;bytes.Buffer&lt;/code&gt; 的說明文件：the zero value for Buffer is an empty buffer ready to use. (Buffer 的零值就是一個空 buffer 並且可以直接使用)。類似地，&lt;code&gt;sync.Mutex&lt;/code&gt; 並沒有建構子，但它的零值就是一個解開 (unlock) 的鎖。&lt;/p&gt;

&lt;p&gt;這個特性可以一個接一個的擴展下去。考慮下列型別：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type SyncedBuffer struct {
    lock    sync.Mutex
    buffer  bytes.Buffer
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這個型只要 &lt;code&gt;new&lt;/code&gt; 完或是宣告完就可以馬上使用 (譯註)。以下的程式碼中，&lt;code&gt;p&lt;/code&gt; 和 &lt;code&gt;v&lt;/code&gt; 都不需要另外再做初始化：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;p := new(SyncedBuffer)  // type *SyncedBuffer
var v SyncedBuffer      // type  SyncedBuffer
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;譯註：因為 &lt;code&gt;SyncedBuffer&lt;/code&gt; 的所有成員都符合這個零值就能使用的特性，所以它本身也可以直接使用。&lt;/p&gt;

&lt;h3 id=&#34;建構子及複合結構的表達式-literal:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;建構子及複合結構的表達式 (literal)&lt;/h3&gt;

&lt;p&gt;有時候零值實在無法符合我們的需求，這時候就要乖乖寫建構子了。以下是從 &lt;code&gt;os&lt;/code&gt; 套件節錄出來的一小段程式：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func NewFile(fd int, name string) *File {
    if fd &amp;lt; 0 {
        return nil
    }
    f := new(File)
    f.fd = fd
    f.name = name
    f.dirinfo = nil
    f.nepipe = 0
    return f
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這寫法真是又臭又長，而我們可以用複合結構表達式來簡化它。複合結構表達式是一段敘述，當執行這段敘述的時候會產生一個實體。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func NewFile(fd int, name string) *File {
    if fd &amp;lt; 0 {
        return nil
    }
    f := File{fd, name, nil, 0}
    return &amp;amp;f
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;有件事值得注意：在 Go 語言中回傳區域變數的位址是很正常的，它不會因為函式結束而被釋放掉。事實上，對複合結構表達式取址的時候，會產生另一個新的實體，所以我們應該把兩行再縮成一行&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;return &amp;amp;File{fd, name, nil, 0}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;當你使用上面那種複合結構表達式的時候，struct 中所有的屬性都必須依順序給予一個值。如果你想省略零值，可以用 &lt;strong&gt;label: value&lt;/strong&gt; 這樣的形式來為非零的屬性賦值。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;return &amp;amp;File{fd: fd, name: name}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如果在複合結構表達式中沒有對任何屬性賦值，那麼產生出來的實體就會是零值。也就是說 &lt;code&gt;new(File)&lt;/code&gt; 跟 &lt;code&gt;&amp;amp;File{}&lt;/code&gt; 是等價的。&lt;/p&gt;

&lt;p&gt;複合結構表達式也可以用來產生 array, slice 或是 map：屬性的標籤是索引 (當然型別必須相符)。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;a := [...]string   {Enone: &amp;quot;no error&amp;quot;, Eio: &amp;quot;Eio&amp;quot;, Einval: &amp;quot;invalid argument&amp;quot;}
s := []string      {Enone: &amp;quot;no error&amp;quot;, Eio: &amp;quot;Eio&amp;quot;, Einval: &amp;quot;invalid argument&amp;quot;}
m := map[int]string{Enone: &amp;quot;no error&amp;quot;, Eio: &amp;quot;Eio&amp;quot;, Einval: &amp;quot;invalid argument&amp;quot;}
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;用-make-來配置:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;用 &lt;em&gt;make&lt;/em&gt; 來配置&lt;/h3&gt;

&lt;p&gt;內建的 &lt;code&gt;make(T, args)&lt;/code&gt; 函式雖然也是配置，但與 &lt;code&gt;new(T)&lt;/code&gt; 完全不同。它是專門用來配置 slice, map 以及 channel 用的，而且它回傳的會是「初始化」過的 &lt;code&gt;T&lt;/code&gt; 型別 (不是 &lt;code&gt;*T&lt;/code&gt; 型別，也不是零值)。這三種型別會參考到一些必須要初始化的內部資料結構，所以需要另外處理。像是 slice，其實是一個資料結構，記載了指標 (指向陣列中的資料)，長度和容量。如果這三者沒有被初始化的話&lt;br /&gt;
， slice 就是 nil。所以 &lt;code&gt;make&lt;/code&gt; 會初始化 slice, map 和 channel 的內部資料結構以備使用。例如：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;make([]int, 10, 100)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;會配置一個承載 int 的 slice，長度是 10，最大容量是 100，並且有一個指標指向最前面的 10 個 int。配置 slice 的時候可以省略容量參數，之後在有關 slice 的章節會有更詳細的討論。反過來說，&lt;code&gt;new([]int)&lt;/code&gt; 回傳的是一個指標，指向了新配置的，填入零值的 slice 內部結構；也就是一個指向 nil slice 的指標。&lt;/p&gt;

&lt;p&gt;以下說明了 &lt;code&gt;new&lt;/code&gt; 和 &lt;code&gt;make&lt;/code&gt; 的差別&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;var p *[]int = new([]int)       // allocates slice structure; *p == nil; rarely useful
var v  []int = make([]int, 100) // the slice v now refers to a new array of 100 ints

// Unnecessarily complex:
var p *[]int = new([]int)
*p = make([]int, 100, 100)

// Idiomatic:
v := make([]int, 100)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;要注意的是 &lt;code&gt;make&lt;/code&gt; 只能配置 slice, map 或是 channel，而且它回傳的不是指標。如果一定要指標的話，你得用 &lt;code&gt;new&lt;/code&gt; 去處理。(譯註：像上面那個例子中，比較複雜的那部份一樣)&lt;/p&gt;

&lt;h3 id=&#34;array:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;Array&lt;/h3&gt;

&lt;p&gt;譯註：array 通常譯為陣列，但在 Go 語言中，array 十分容易與 slice (通常譯為片段)搞混。兩者在 Go 語言中的意涵與其他語言不大相同，可以算是專有名詞，故以下都不做翻譯，藉此提升讀者對這兩個名詞的熟悉度。而「陣列」一詞則會泛指類 C 語言中的陣列的行為。&lt;/p&gt;

&lt;p&gt;Array 在規劃記憶體的時候相當實用，有時候還可以用來避免不必要的資源配置動作。不過它最主要的功能還是做為 slice 的基底，slice 在下一節會討論。做為 slice 的先備知識，這裡提出一些 array 的特點。&lt;/p&gt;

&lt;p&gt;Array 在 Go 和 C 語言之間有些重大的不同：&lt;/p&gt;

&lt;ul&gt;
&lt;li&gt;Go 語言的 array 是單純的值。把 array 指定給變數的時候會複製一份副本。&lt;br /&gt;&lt;/li&gt;
&lt;li&gt;同樣的，把 array 當成參數傳入函式的時候，函式內收到的會是副本。&lt;br /&gt;&lt;/li&gt;
&lt;li&gt;array 的長度是型別的一部份，也就是說 &lt;code&gt;[10]int&lt;/code&gt; 和 &lt;code&gt;[20]int&lt;/code&gt; 屬於不同型別。&lt;br /&gt;&lt;/li&gt;
&lt;/ul&gt;

&lt;p&gt;Array 是值這件事有時候很有用，有時候卻代價高昂。如果你需要類似 C 語言陣列那樣的行為，你可以用指標：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func Sum(a *[3]float64) (sum float64) {
    for _, v := range *a {
        sum += v
    }
    return
}

array := [...]float64{7.0, 8.5, 9.1}
x := Sum(&amp;amp;array)  // Note the explicit address-of operator
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;不過這不是 Go 語言的慣例，我們應該用 slice。&lt;/p&gt;

&lt;h3 id=&#34;slice:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;Slice&lt;/h3&gt;

&lt;p&gt;Slice 可以說是在 array 外面包了一層，提供更方便有效的介面存取一連串的資料。除了像是處理矩陣這種事之外，在 Go 語言中大部份的陣列操作都是用 slice 完成的。&lt;/p&gt;

&lt;p&gt;Slice 會有一個指向內部 array 的指標，如果你把一個 slice 指定給另一個，兩個都會指向同一個 array。如果你把 slice 當成參數傳入函式，那麼在函式內部對 slice 元素做的改變，函式之外也會存取得到，如同你傳入的是指標一樣。所以 &lt;code&gt;Read&lt;/code&gt; 函式接受 slice 當參數，而不是一個指標配上一個整數：slice 的長度指出了有多少資料可以讀取。以下是在 &lt;code&gt;os&lt;/code&gt; 套件中，&lt;code&gt;File&lt;/code&gt; 型別對於 &lt;code&gt;Read&lt;/code&gt; 方法的定義：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func (f *File) Read(buf []byte) (n int, err error)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這個方法回傳「讀到了幾 bytes」和一個錯誤值 (如果有錯誤的話)。如果你想把資料讀進某個緩衝區的前 32 bytes，就該把它切成 slice：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;n, err := f.Read(buf[0:32])
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這種用法很常見，效率也很高。老實說，如果不考慮效率的話，以下的程式碼也可以達成相同的事&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;var n int
var err error
for i := 0; i &amp;lt; 32; i++ {
    nbytes, e := f.Read(buf[i:i+1])  // Read one byte.
    if nbytes == 0 || e != nil {
        err = e
        break
    }
    n += nbytes
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;在不超出內部 array 的範圍之內，你可以任意的改變 slice 的長度：只要把它切成另一個 slice 就好了。而 slice 的容量可以透過內建的 &lt;code&gt;cap&lt;/code&gt; 函式取得，它會回傳這個 slice 目前可能的最大長度。以下是一個範例，它可以把資料新增到 slice 的末端；如果超過 slice 容量的話，也會為你重新配置適合的大小，最後把新的 slice 傳回來。這個範例使用了 &lt;code&gt;len&lt;/code&gt; 和 &lt;code&gt;cap&lt;/code&gt; 這兩個函式的特性：nil slice 的長度和容量都是 0。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func Append(slice, data []byte) []byte {
    l := len(slice)
    if l + len(data) &amp;gt; cap(slice) {  // reallocate
        // Allocate double what&#39;s needed, for future growth.
        newSlice := make([]byte, (l+len(data))*2)
        // The copy function is predeclared and works for any slice type.
        copy(newSlice, slice)
        slice = newSlice
    }
    slice = slice[0:l+len(data)]
    for i, c := range data {
        slice[l+i] = c
    }
    return slice
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;在這個範例中，我們一定得把新的 slice 傳回去。雖然我們可以 (透過指標) 直接修改 slice 裡的資料，但是在這裡我們要變更的是 slice 本身 (也就是指向 array 的指標、slice 的長度及容量等資料)，而這些資料都是單純的值，所以在函式內只能收到副本。&lt;/p&gt;

&lt;p&gt;這個範例實用到我們決定內建一個類似的函式 &lt;code&gt;append&lt;/code&gt;。我們還需要了解一些其他的知識才能理解 &lt;code&gt;append&lt;/code&gt; 的設計，所以之後的章節會再次討論 &lt;code&gt;append&lt;/code&gt;。&lt;/p&gt;

&lt;h3 id=&#34;二維的-slice:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;二維的 slice&lt;/h3&gt;

&lt;p&gt;Array 和 slice 都是一維的。Go 語言中所謂二維的 array 或是 slice，其實是「裝載 array 的 array」以及「裝載 slice 的 slice」：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type Transform [3][3]float64  // A 3x3 array, really an array of arrays.
type LinesOfText [][]byte     // A slice of byte slices.
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;而因為 slice 的大小是可以動態改變的，這意味著「被裝載在 slice 裡的那些 slice」不需要有相同的長度或容量。這寫法還滿常用的，以上面那個 LinesOfText 作例子：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;text := LinesOfText{
	[]byte(&amp;quot;Now is the time&amp;quot;),
	[]byte(&amp;quot;for all good gophers&amp;quot;),
	[]byte(&amp;quot;to bring some fun to the party.&amp;quot;),
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;有時候這種技巧是必要的，比如掃描一個圖形中每一列裡的每個點。有兩種方法可以達成這件事：你可以把為每一列都配置一個 slice，或是依序把每個點放進一個 array 裡，然後把每一列都切成不同的 slice。兩種方式各有優缺點，端看你的程式適合的是那一種。如果 slice 的長度會變的話，第一種方法可以避免覆蓋到不同列的資料。如果不會變的話，第二種方法可以避免一再配置記憶體造成的效能損耗。以下是這兩種做法的示意，首先是第一種做法：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// Allocate the top-level slice.
picture := make([][]uint8, YSize) // One row per unit of y.
// Loop over the rows, allocating the slice for each row.
for i := range picture {
	picture[i] = make([]uint8, XSize)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;然後是第二種：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// Allocate the top-level slice, the same as before.
picture := make([][]uint8, YSize) // One row per unit of y.
// Allocate one large slice to hold all the pixels.
pixels := make([]uint8, XSize*YSize) // Has type []uint8 even though picture is [][]uint8.
// Loop over the rows, slicing each row from the front of the remaining pixels slice.
for i := range picture {
	picture[i], pixels = pixels[:XSize], pixels[XSize:]
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;map:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;Map&lt;/h3&gt;

&lt;p&gt;Map 是非常好用的內建型別，它將某種型別的值 (這個值稱為索引) 跟另一種型別的值 (稱為元素，或是直接稱為值) 配成一對。任何可以使用 &lt;code&gt;==&lt;/code&gt; 比對的型別都可以當做索引，像是整數、浮點數、複數、字串、指標、介面 (如果這個介面可以用 &lt;code&gt;==&lt;/code&gt; 比對的話)、struct 或 array。Slice 不能用來當索引，因為它不支援 &lt;code&gt;==&lt;/code&gt;。Map 也像 slice 那樣，有個內部的資料結構。如果你把 map 當成參數傳入某個函式中，在函式內修改 map 的元素也會反映到函式外。&lt;/p&gt;

&lt;p&gt;你可以用複合結構表達式來初始化一個 map 的元素，語法很簡單：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;var timeZone = map[string]int{
    &amp;quot;UTC&amp;quot;:  0*60*60,
    &amp;quot;EST&amp;quot;: -5*60*60,
    &amp;quot;CST&amp;quot;: -6*60*60,
    &amp;quot;MST&amp;quot;: -7*60*60,
    &amp;quot;PST&amp;quot;: -8*60*60,
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;賦值和取值的語法跟 array 或 slice 一樣，只是索引的型別不限於整數。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;offset := timeZone[&amp;quot;EST&amp;quot;]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如果索引不存在的話，你會取到零值。所以集合 (數學中的 Set) 可以用 &lt;code&gt;map[某型別]bool&lt;/code&gt; 來實現。當你要把某個元素加進集合的時候，就把那個元素所索引到的值設成 &lt;code&gt;true&lt;/code&gt;。這樣透過對 map 取值的動作就可以知道某元素是否在集合中：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;attended := map[string]bool{
    &amp;quot;Ann&amp;quot;: true,
    &amp;quot;Joe&amp;quot;: true,
    ...
}

if attended[person] { // will be false if person is not in the map
    fmt.Println(person, &amp;quot;was at the meeting&amp;quot;)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;有時候我們得確實分辨究竟是索引不存在還是這個值剛好是零值：上方 timeZone 的例子裡，&lt;code&gt;timezone[&amp;quot;UTC&amp;quot;]&lt;/code&gt; 是不存在還是剛好為零呢？我們可以用多重傳回值的方式確定：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;var seconds int
var ok bool
seconds, ok = timeZone[tz]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這個慣例很直觀的稱為「逗號 ok」。在這個例子裡，如果 tz 這個索引存在，second 會取到值，ok 會是 true。如果 tz 不存在，second 會是零值，而 ok 會是 false。底下的例子總合了之前我們介紹的東西來實作錯誤回報機制：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func offset(tz string) int {
    if seconds, ok := timeZone[tz]; ok {
        return seconds
    }
    log.Println(&amp;quot;unknown time zone:&amp;quot;, tz)
    return 0
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如果只是要測試索引是否存在，你可以用空白識別子：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;_, present := timeZone[tz]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;要刪除某個索引，內建的 &lt;code&gt;delete&lt;/code&gt; 函式可以做到。它的參數是 map 跟要刪除的索引。索引不存在的狀況下呼叫 &lt;code&gt;delete&lt;/code&gt; 並不會造成錯誤。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;delete(timeZone, &amp;quot;PDT&amp;quot;)  // Now on Standard Time
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;格式化輸出:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;格式化輸出&lt;/h3&gt;

&lt;p&gt;(譯註：原文 printing 通常譯為列印，但本節內容係介紹類似 C 語言中 printf 的函式，故譯作格式化輸出)&lt;/p&gt;

&lt;p&gt;Go 語言中的格式化輸出函式跟 C 語言中的 &lt;code&gt;printf&lt;/code&gt; 系列函式很像，但支援了更多語法。相關的函式都在 &lt;code&gt;fmt&lt;/code&gt; 套件中，函式名稱的首字母都是大寫，比如 &lt;code&gt;fmt.Printf&lt;/code&gt;, &lt;code&gt;fmt.Fprintf&lt;/code&gt; 等等。像 &lt;code&gt;Sprintf&lt;/code&gt; 等處理字串的函式會回傳一個新的字串，不會修改原有的字串。&lt;/p&gt;

&lt;p&gt;格式字串不一定是必需的，對於 &lt;code&gt;Printf&lt;/code&gt;, &lt;code&gt;Fprintf&lt;/code&gt; 和 &lt;code&gt;Sprintf&lt;/code&gt;，各自都有對應的、不需要格式化字串的版本，比如 &lt;code&gt;Print&lt;/code&gt; 和 &lt;code&gt;Println&lt;/code&gt;。這些特殊版本不需格式字串，而是把每一個參數照預設的格式輸出。&lt;code&gt;Println&lt;/code&gt; 還會在輸出每個參數之後加個換行符號，而 &lt;code&gt;Print&lt;/code&gt; 則是會在兩個字串參數之間空一格。以下四行程式的輸出是一樣的&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;fmt.Printf(&amp;quot;Hello %d\n&amp;quot;, 23)
fmt.Fprint(os.Stdout, &amp;quot;Hello &amp;quot;, 23, &amp;quot;\n&amp;quot;)
fmt.Println(&amp;quot;Hello&amp;quot;, 23)
fmt.Println(fmt.Sprint(&amp;quot;Hello &amp;quot;, 23))
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;而 &lt;code&gt;fmt.Fprint&lt;/code&gt; 系列則需要一個特別的參數：第一個參數必需要是實作 &lt;code&gt;io.Writer&lt;/code&gt; 介面的任意物件，比如 &lt;code&gt;os.Stdout&lt;/code&gt; 和 &lt;code&gt;os.Stderr&lt;/code&gt;。&lt;/p&gt;

&lt;p&gt;接下來開始跟 C 語言產生分岐了。首先，數值格式，像是 &lt;code&gt;%d&lt;/code&gt;，不接受正負號或是位元長度的修飾詞，Go 會透過參數的型別來決定該輸出什麼。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;var x uint64 = 1&amp;lt;&amp;lt;64 - 1
fmt.Printf(&amp;quot;%d %x; %d %x\n&amp;quot;, x, x, int64(x), int64(x))
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;會輸出&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;18446744073709551615 ffffffffffffffff; -1 -1
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如果你想指定預設格式，可以用 &lt;code&gt;%v&lt;/code&gt;，輸出的格式會和用 &lt;code&gt;Print&lt;/code&gt; 或 &lt;code&gt;Println&lt;/code&gt; 函式的結果一樣。同樣地，&lt;code&gt;%v&lt;/code&gt; 可以接受 &lt;strong&gt;任何&lt;/strong&gt; 型別，就算是 array, slice, map 和 struct 都沒問題。以上一節的時區 map 為例：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;fmt.Printf(&amp;quot;%v\n&amp;quot;, timeZone)  // or just fmt.Println(timeZone)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;輸出會是&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;map[CST:-21600 PST:-28800 EST:-18000 UTC:0 MST:-25200]
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;輸出 map 的時候，索引順序不是固定的。在輸出 struct 的時候，你可以用 &lt;code&gt;%+v&lt;/code&gt; 來一併輸出屬性名稱。而 &lt;code&gt;%#v&lt;/code&gt; 則是輸出任何型別的完整 Go 語法。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type T struct {
    a int
    b float64
    c string
}
t := &amp;amp;T{ 7, -2.35, &amp;quot;abc\tdef&amp;quot; }
fmt.Printf(&amp;quot;%v\n&amp;quot;, t)
fmt.Printf(&amp;quot;%+v\n&amp;quot;, t)
fmt.Printf(&amp;quot;%#v\n&amp;quot;, t)
fmt.Printf(&amp;quot;%#v\n&amp;quot;, timeZone)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;會輸出 (注意那個取值符號 &lt;code&gt;&amp;amp;&lt;/code&gt;)&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;&amp;amp;{7 -2.35 abc   def}
&amp;amp;{a:7 b:-2.35 c:abc     def}
&amp;amp;main.T{a:7, b:-2.35, c:&amp;quot;abc\tdef&amp;quot;}
map[string] int{&amp;quot;CST&amp;quot;:-21600, &amp;quot;PST&amp;quot;:-28800, &amp;quot;EST&amp;quot;:-18000, &amp;quot;UTC&amp;quot;:0, &amp;quot;MST&amp;quot;:-25200}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;code&gt;%q&lt;/code&gt; 會輸出 Go 的字串語法，所以它接受的是字串和 []byte 型別。&lt;code&gt;%#q&lt;/code&gt; 跟 &lt;code&gt;%q&lt;/code&gt; 差不多，只是它用的不是雙引號，而是反引號。其實 &lt;code&gt;%q&lt;/code&gt; 系列也接受整數和 rune，只是輸出的是用單引號包起來的單一 rune。還有 &lt;code&gt;%x&lt;/code&gt; 可以接受字串、[]byte 和整數。&lt;code&gt;% x&lt;/code&gt; 則會在每個 byte 之間加上空白。&lt;/p&gt;

&lt;p&gt;另一個很好用的格式是 &lt;code&gt;%T&lt;/code&gt;，會輸出參數的型別&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;fmt.Printf(&amp;quot;%T\n&amp;quot;, timeZone)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;會輸出&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;map[string] int
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如果你想要控制某個自訂型別的預設輸出格式，只要為那個型別定義一個 &lt;code&gt;String() string&lt;/code&gt; 方法就好。比如有某個自訂型別 T&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func (t *T) String() string {
    return fmt.Sprintf(&amp;quot;%d/%g/%q&amp;quot;, t.a, t.b, t.c)
}
fmt.Printf(&amp;quot;%v\n&amp;quot;, t)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;輸出&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;7/-2.35/&amp;quot;abc\tdef&amp;quot;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如果你同時要控制 &lt;code&gt;T&lt;/code&gt; 和 &lt;code&gt;*T&lt;/code&gt; 的格式，那 &lt;code&gt;String&lt;/code&gt; 方法的接收器必須是 &lt;code&gt;T&lt;/code&gt; 型別；上面的範例用 &lt;code&gt;*T&lt;/code&gt; 型別的原因是，對 struct 使用指標是慣例，因為這樣效率比較高。這在 &lt;code&gt;指標與數值接收器&lt;/code&gt; 的章節會有更深入的討論&lt;/p&gt;

&lt;p&gt;在我們自訂的 &lt;code&gt;String&lt;/code&gt; 方法中可以呼叫 &lt;code&gt;Sprintf&lt;/code&gt; 函式，這是因為 &lt;code&gt;Print&lt;/code&gt; 系列函式都是可重複進入的。只有一種情況要避免：在 &lt;code&gt;String&lt;/code&gt; 方法裡使用 &lt;code&gt;Sprintf&lt;/code&gt; 的時候，不慎讓它又自動呼叫了同一個 &lt;code&gt;String&lt;/code&gt; 方法。如果你要求 &lt;code&gt;Sprintf&lt;/code&gt; 輸出接收到的 struct，就會發生這種事。這是相當常見的錯誤，下面是錯誤示範。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type MyString string

func (m MyString) String() string {
    return fmt.Sprintf(&amp;quot;MyString=%s&amp;quot;, m) // Error: will recur forever.
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;不過這很容易修正：強制轉成字串型別 (因為字串型別裡不會有我們自訂的 &lt;code&gt;String&lt;/code&gt; 方法) 就好了&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type MyString string
func (m MyString) String() string {
    return fmt.Sprintf(&amp;quot;MyString=%s&amp;quot;, string(m)) // OK: note conversion.
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;在 &lt;em&gt;初始化&lt;/em&gt; 的章節中，我們會討論另一種技巧來避免這個無限遞迴問題。&lt;/p&gt;

&lt;p&gt;還有一個技巧，是把要輸出的參數直接轉傳給另一個輸出函式。&lt;code&gt;Printf&lt;/code&gt; 的定義中用了 &lt;code&gt;...interface{}&lt;/code&gt; 作為最後的參數，代表了它可以接受任意數量、任意型別的參數。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func Printf(format string, v ...interface{}) (n int, err error) {
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;在 &lt;code&gt;Printf&lt;/code&gt; 函式裡，參數 &lt;code&gt;v&lt;/code&gt; 是 &lt;code&gt;[]interface{}&lt;/code&gt; 型別。但如果把它傳給其他參數數量不固定的函式時，卻又會像是參數列表一樣。以下是 &lt;code&gt;log.Println&lt;/code&gt; 的實作方式，它把參數轉傳給 &lt;code&gt;fmt.Sprintln&lt;/code&gt; 以取得預需的輸出格式。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// Println prints to the standard logger in the manner of fmt.Println.
func Println(v ...interface{}) {
    std.Output(2, fmt.Sprintln(v...))  // Output takes parameters (int, string)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;我們呼叫 &lt;code&gt;Sprintln&lt;/code&gt; 的時候用了 &lt;code&gt;v...&lt;/code&gt; 這樣的語法，讓編譯器知道我們想把 &lt;code&gt;v&lt;/code&gt; 當成是參數列表。否則 &lt;code&gt;v&lt;/code&gt; 會被當成單一個 &lt;code&gt;[]interface{}&lt;/code&gt; 型別的參數。&lt;/p&gt;

&lt;p&gt;其實還有很多相關的東西我們這裡沒有提到，細節你可以看 &lt;code&gt;godoc&lt;/code&gt; 產生的 &lt;code&gt;fmt&lt;/code&gt; 套件說明文件。&lt;/p&gt;

&lt;p&gt;題外話，&lt;code&gt;...&lt;/code&gt; 語法可以用在任何 slice 上，例如你想寫一個函式，從數個整數中找出最小的值&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func Min(a ...int) int {
    min := int(^uint(0) &amp;gt;&amp;gt; 1)  // largest int
    for _, i := range a {
        if i &amp;lt; min {
            min = i
        }
    }
    return min
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;append:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;Append&lt;/h3&gt;

&lt;p&gt;現在我們已經準備好要解決 &lt;code&gt;append&lt;/code&gt; 函式最後的謎團。內建的 &lt;code&gt;append&lt;/code&gt; 函式的定義和上面我們自訂的 &lt;code&gt;Append&lt;/code&gt; 函式不一樣。概念上，&lt;code&gt;append&lt;/code&gt; 的定義可以說是&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func append(slice []T, elements ...T) []T
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;code&gt;T&lt;/code&gt; 只是一個記號，代表了你想要的型別。但這不是正確的 Go 語法。函式的定義裡，所有的型別都要是確定的，不能在呼叫時隨著需要改變。這就是我們內建 &lt;code&gt;append&lt;/code&gt; 函式的原因：這件事需要編譯器來處理。&lt;/p&gt;

&lt;p&gt;&lt;code&gt;append&lt;/code&gt; 函式會把傳進來的參數加到 slice 的後面然後傳回去。之所以要把 slice 回傳的原因在之前已經提過：它會更動內部的資料結構。以下這個簡單的範例&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;x := []int{1,2,3}
x = append(x, 4, 5, 6)
fmt.Println(x)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;會輸出 &lt;code&gt;[1 2 3 4 5 6]&lt;/code&gt;。你應該會發現這點跟 &lt;code&gt;Printf&lt;/code&gt; 很像，都可以接受任意數量的參數。&lt;/p&gt;

&lt;p&gt;那如果我們把一個 slice 裡的所有元素依序加到另一個 slice 的後面呢？很簡單，就用我們剛剛才提到的 &lt;code&gt;...&lt;/code&gt;。所以下面這段程式碼和上面的例子會有一樣的結果：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;x := []int{1,2,3}
y := []int{4,5,6}
x = append(x, y...)
fmt.Println(x)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如果沒有 &lt;code&gt;...&lt;/code&gt; 的話就會發生編譯錯誤，因為 &lt;code&gt;y&lt;/code&gt; 的型別是 &lt;code&gt;[]int&lt;/code&gt; 而不是 &lt;code&gt;int&lt;/code&gt;。&lt;/p&gt;

&lt;h2 id=&#34;初始化:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;初始化&lt;/h2&gt;

&lt;p&gt;雖然 Go 語言的初始化看起來跟 C/C++ 的差別不大，但事實上它強大多了。初始化的時候可以建立複雜的結構，在物件之間，甚至是套件之間的順序問題也能正確的解決。&lt;/p&gt;

&lt;h3 id=&#34;常數:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;常數&lt;/h3&gt;

&lt;p&gt;在 Go 語言裡，常數就是個常數。不論是全域的還是區域的，它們在編譯的時候就已經產生好了，型別也只能是數值、字元、rune、字串或是 bool。因為編譯時期的限制，定義常數只能用靜態表達式，而編譯器會執行這些表達式。舉例來說， &lt;code&gt;1&amp;lt;&amp;lt;3&lt;/code&gt; 是靜態表達式，但 &lt;code&gt;math.Sin(math.Pi/4)&lt;/code&gt; 則不是：因為要到執行的時候才會做函式呼叫。&lt;/p&gt;

&lt;p&gt;一系列的常數可以用 &lt;code&gt;iota&lt;/code&gt; 運算元來定義。&lt;code&gt;iota&lt;/code&gt; 可以是敘述的一部份，而敘述可以省略，所以定義起來很省事：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type ByteSize float64

const (
    _           = iota // ignore first value by assigning to blank identifier
    KB ByteSize = 1 &amp;lt;&amp;lt; (10 * iota)
    MB
    GB
    TB
    PB
    EB
    ZB
    YB
)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;由於你可以為任何自訂型別加上自訂的方法，所以透過加上 &lt;code&gt;String&lt;/code&gt; 方法，我們就可以讓常數有個漂亮的預設格式。這個技巧對於單純的數值型別也很有用，比如原本是浮點數的 &lt;code&gt;ByteSize&lt;/code&gt; 型別。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func (b ByteSize) String() string {
    switch {
    case b &amp;gt;= YB:
        return fmt.Sprintf(&amp;quot;%.2fYB&amp;quot;, b/YB)
    case b &amp;gt;= ZB:
        return fmt.Sprintf(&amp;quot;%.2fZB&amp;quot;, b/ZB)
    case b &amp;gt;= EB:
        return fmt.Sprintf(&amp;quot;%.2fEB&amp;quot;, b/EB)
    case b &amp;gt;= PB:
        return fmt.Sprintf(&amp;quot;%.2fPB&amp;quot;, b/PB)
    case b &amp;gt;= TB:
        return fmt.Sprintf(&amp;quot;%.2fTB&amp;quot;, b/TB)
    case b &amp;gt;= GB:
        return fmt.Sprintf(&amp;quot;%.2fGB&amp;quot;, b/GB)
    case b &amp;gt;= MB:
        return fmt.Sprintf(&amp;quot;%.2fMB&amp;quot;, b/MB)
    case b &amp;gt;= KB:
        return fmt.Sprintf(&amp;quot;%.2fKB&amp;quot;, b/KB)
    }
    return fmt.Sprintf(&amp;quot;%.2fB&amp;quot;, b)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這樣 &lt;code&gt;YB&lt;/code&gt; 會輸出 &lt;code&gt;1.00YB&lt;/code&gt;，而 &lt;code&gt;ByteSize(1e13)&lt;/code&gt; 則是 &lt;code&gt;9.09TB&lt;/code&gt;。&lt;/p&gt;

&lt;p&gt;這段程式碼不會造成無限遞迴，因為 &lt;code&gt;%f&lt;/code&gt; 不會呼叫到 &lt;code&gt;String&lt;/code&gt; - 它接受的是浮點數而非字串。&lt;/p&gt;

&lt;h3 id=&#34;變數:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;變數&lt;/h3&gt;

&lt;p&gt;變數也可以初始化，而且可以使用一般的敘述。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;var (
    home   = os.Getenv(&amp;quot;HOME&amp;quot;)
    user   = os.Getenv(&amp;quot;USER&amp;quot;)
    gopath = os.Getenv(&amp;quot;GOPATH&amp;quot;)
)
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;init-函式:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;&lt;em&gt;init&lt;/em&gt; 函式&lt;/h3&gt;

&lt;p&gt;最後，每個源碼檔都可以定義自己的 &lt;code&gt;init&lt;/code&gt; 函式，這個函式不接受任何參數。事實上，每個源碼檔可以有複數個 &lt;code&gt;init&lt;/code&gt; 函式。&lt;code&gt;init&lt;/code&gt; 是整個初始化環節的最後一步：要等到套件中為變數初始化的敘述都執行完，而且 &lt;code&gt;import&lt;/code&gt; 進來的套件也初始化完畢之後，才會執行 &lt;code&gt;init&lt;/code&gt;。&lt;/p&gt;

&lt;p&gt;除了進行一些無法簡單地用敘述完成的初始化工作之外，另外一種常見的用法是在 &lt;code&gt;init&lt;/code&gt; 裡確認套件的狀態，或在必要時進行修復&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func init() {
    if user == &amp;quot;&amp;quot; {
        log.Fatal(&amp;quot;$USER not set&amp;quot;)
    }
    if home == &amp;quot;&amp;quot; {
        home = &amp;quot;/home/&amp;quot; + user
    }
    if gopath == &amp;quot;&amp;quot; {
        gopath = home + &amp;quot;/go&amp;quot;
    }
    // gopath may be overridden by --gopath flag on command line.
    flag.StringVar(&amp;amp;gopath, &amp;quot;gopath&amp;quot;, gopath, &amp;quot;override default GOPATH&amp;quot;)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h2 id=&#34;方法:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;方法&lt;/h2&gt;

&lt;h3 id=&#34;指標與值:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;指標與值&lt;/h3&gt;

&lt;p&gt;回想一下我們先前提過的 &lt;code&gt;ByteSize&lt;/code&gt;，我們可以為任何型別定義方法 (除了指標型別或是介面型別)；接收器不一定要是 struct。&lt;/p&gt;

&lt;p&gt;先前討論 slice 的時候，我們寫了一個 &lt;code&gt;Append&lt;/code&gt; 函式作為範例。現在我們要試著把它改寫成 slice 的一個方法。首先，我們得要定義一個有命名的型別，這樣才能為它加上方法。至於方法的接收器，當然是我們剛定義的型別了。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type ByteSlice []byte

func (slice ByteSlice) Append(data []byte) []byte {
    // Body exactly the same as above
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這樣還是得回傳一個新的 slice 回去，我們可以把接收器改成指標，那麼我們就可以在方法裡改變 slice。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func (p *ByteSlice) Append(data []byte) {
    slice := *p
    // Body as above, without the return.
    *p = slice
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這樣就完美了嗎？其實還可以寫得更好。如果我們把方法的定義改成像標準的 &lt;code&gt;Write&lt;/code&gt; 方法的話：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func (p *ByteSlice) Write(data []byte) (n int, err error) {
    slice := *p
    // Again as above.
    *p = slice
    return len(data), nil
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;那麼 &lt;code&gt;*ByteSlice&lt;/code&gt; 這個型別就滿足了 &lt;code&gt;io.Writer&lt;/code&gt; 這個介面的要求 (譯註：就是實作了這個介面的意思), 這下可有趣了。比如說，現在我們可以把 &lt;code&gt;*ByteSlice&lt;/code&gt; 當成串流來寫入。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;var b ByteSlice
fmt.Fprintf(&amp;amp;b, &amp;quot;This hour has %d days\n&amp;quot;, 7)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;注意那個取址符號，因為只有 &lt;code&gt;*ByteSlice&lt;/code&gt; 型別實作了 &lt;code&gt;io.Writer&lt;/code&gt;。如果你的接收器是接收值的，那麼實體不論是值型別 (&lt;code&gt;T&lt;/code&gt;) 還是指標型別 (&lt;code&gt;*T&lt;/code&gt;)，都可以呼叫這個方法；當接收器是接收指標的時候，只有在指標型別裡才有定義這個方法。&lt;/p&gt;

&lt;p&gt;這個規則是因為用指標型別的話就可以修改原本的實體，然而 Go 在傳遞值的時候會產生副本，這代表你在方法中做的變更其實都改到副本上。所以我們直接在語言層面上杜絕發生這種錯誤的可能。對於接收指標的方法，如果呼叫的時候需要而且可以取址，那麼 Go 會幫你自動加上取址符號。比如上面的例子，我們可以直接寫 &lt;code&gt;b.Write&lt;/code&gt;，而 Go 會幫我們把它改成 &lt;code&gt;(&amp;amp;b).Write&lt;/code&gt;。&lt;/p&gt;

&lt;p&gt;題外話，上面那個改寫成 &lt;code&gt;Write&lt;/code&gt; 的主意就是我們在實作 &lt;code&gt;bytes.Buffer&lt;/code&gt; 時的中心思想。&lt;/p&gt;

&lt;h2 id=&#34;介面以及其他型別:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;介面以及其他型別&lt;/h2&gt;

&lt;h3 id=&#34;介面:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;介面&lt;/h3&gt;

&lt;p&gt;在 Go 語言中，介面是用來指定物件的行為：如果某個物件可以做「這件事」，那麼它就可以用來當成是「這種東西」。先前我們已經看到好幾個簡單的例子：我們可以透過 &lt;code&gt;String&lt;/code&gt; 方法來實作自訂的輸出，而 &lt;code&gt;Fprintf&lt;/code&gt; 可以輸出到任何有實作 &lt;code&gt;Write&lt;/code&gt; 方法的物件裡。&lt;/p&gt;

&lt;p&gt;一個型別可以實作好幾個介面。舉例來說，一個 collection 可以藉由實作 &lt;code&gt;sort.Interface&lt;/code&gt; 這個介面來讓 &lt;code&gt;sort&lt;/code&gt; 套件為它排序，同時也可以提供一個自訂的輸出格式：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type Sequence []int

// Methods required by sort.Interface.
func (s Sequence) Len() int {
    return len(s)
}
func (s Sequence) Less(i, j int) bool {
    return s[i] &amp;lt; s[j]
}
func (s Sequence) Swap(i, j int) {
    s[i], s[j] = s[j], s[i]
}

// Method for printing - sorts the elements before printing.
func (s Sequence) String() string {
    sort.Sort(s)
    str := &amp;quot;[&amp;quot;
    for i, elem := range s {
        if i &amp;gt; 0 {
            str += &amp;quot; &amp;quot;
        }
        str += fmt.Sprint(elem)
    }
    return str + &amp;quot;]&amp;quot;
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;型別轉換:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;型別轉換&lt;/h3&gt;

&lt;p&gt;上面的 &lt;code&gt;String&lt;/code&gt; 方法其實是把 &lt;code&gt;Sprint&lt;/code&gt; 函式對 slice 做的事重寫了一遍。我們可以把 &lt;code&gt;Sequence&lt;/code&gt; 轉成 &lt;code&gt;[]int&lt;/code&gt; 型別來享受 &lt;code&gt;fmt&lt;/code&gt; 套件的勞動成果。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func (s Sequence) String() string {
    sort.Sort(s)
    return fmt.Sprint([]int(s))
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這也是靠型別轉換來避免在 &lt;code&gt;String&lt;/code&gt; 方法中產生無限遞迴的好例子。因為 &lt;code&gt;Sequence&lt;/code&gt; 跟 &lt;code&gt;[]int&lt;/code&gt; 本質上是同樣的型別，所以它們彼此之間可以安全轉換。型別的轉換不會產生新的實體，只是暫時假裝它是另一種相容的型別而已。此外也有會產生新的實體的型別轉換，比如整數和浮點數之間的轉換。&lt;/p&gt;

&lt;p&gt;靠型別轉換來存取不同的方法，這是 Go 的慣例。比如我們可以用 &lt;code&gt;sort.IntSlice&lt;/code&gt; 把上面的例子整個簡化成&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type Sequence []int

// Method for printing - sorts the elements before printing
func (s Sequence) String() string {
    sort.IntSlice(s).Sort()
    return fmt.Sprint([]int(s))
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;原本我們是在 &lt;code&gt;Sequence&lt;/code&gt; 實作特定介面來達成排序的需求，現在是利用型別轉換來取用在 &lt;code&gt;sort.IntSlice&lt;/code&gt; 中定義的排序方法。這樣的技巧在實際環境中比較少用，但是非常有效率。&lt;/p&gt;

&lt;h3 id=&#34;介面的轉換以及型別斷言:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;介面的轉換以及型別斷言&lt;/h3&gt;

&lt;p&gt;早先提到的 type switch 也是一種型別轉換：針對某個介面的實體，視它符合哪個 &lt;code&gt;case&lt;/code&gt; 所指定的型別，然後把它轉換成那個型別。以下是一個簡化的例子，示範了 &lt;code&gt;fmt.Prinf&lt;/code&gt; 是如何用 type switch 把值變成字串。如果值本來就是字串，那就直接輸出不用轉換；如果它有實作 &lt;code&gt;String&lt;/code&gt; 方法，那就用 &lt;code&gt;String&lt;/code&gt; 方法回傳的結果。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type Stringer interface {
    String() string
}

var value interface{} // Value provided by caller.
switch str := value.(type) {
case string:
    return str
case Stringer:
    return str.String()
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;第一個 &lt;code&gt;case&lt;/code&gt; 配對到實值，第二個則是轉成 &lt;code&gt;Stringer&lt;/code&gt; 介面。像這樣子混用型別是安全的。&lt;/p&gt;

&lt;p&gt;如果我們只想要某一種型別呢？比如我很確定這個值一定會是字串，而我想直接取得它的值呢？只有一個 &lt;code&gt;case&lt;/code&gt; 的 type switch 當然是個解法，但型別斷言也行。型別斷言會從一個介面中擷取出特定型別的值來。型別斷言的語法和 type switch 很像，只是把 &lt;code&gt;type&lt;/code&gt; 關鍵字換成了型別：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;value.(typeName)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;結果會是一個新的值，型別是 &lt;code&gt;typeName&lt;/code&gt;。這個 &lt;code&gt;typeName&lt;/code&gt; 必須是這個介面的實體本身的型別，或是另一個可以安全轉換的介面。如果我們知道 &lt;code&gt;value&lt;/code&gt; 真的會是字串的話：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;str := value.(string)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;但如果不是的話，這會造成執行時期的錯誤。你可以用「逗號 ok」這個慣例來確認。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;str, ok := value.(string)
if ok {
    fmt.Printf(&amp;quot;string value is: %q\n&amp;quot;, str)
} else {
    fmt.Printf(&amp;quot;value is not a string\n&amp;quot;)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如果型別斷言失敗，&lt;code&gt;str&lt;/code&gt; 仍然會是字串型別，但值會是零值，也就是空字串。&lt;/p&gt;

&lt;p&gt;作為示意，以下是用 &lt;em&gt;if-else&lt;/em&gt; 實作的 type switch&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;if str, ok := value.(string); ok {
    return str
} else if str, ok := value.(Stringer); ok {
    return str.String()
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;一般性:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;一般性&lt;/h3&gt;

&lt;p&gt;如果某個型別完全只是為了實作特定介面而存在的，那我們完全不必讓它可以公開存取，只要讓介面可以公開存取就夠了。這樣可以明白表示重要的是它的行為，而不是實作它的那個東西；這讓其他具有不同特性的實作方式也能保持相同的行為。同時也讓你避過了一份說明要寫兩次的痛苦。&lt;/p&gt;

&lt;p&gt;在這種情況下，建構子應當要回傳介面。比如在 hash 相關的套件中， &lt;code&gt;crc32.NewIEEE&lt;/code&gt; 與 &lt;code&gt;adler32.New&lt;/code&gt; 都是回傳 &lt;code&gt;hash.Hash32&lt;/code&gt; 介面。所以如果你的程式會用到 crc32，而事後想要換成 adler32，只要把建構子換掉就好了，其他部份的程式碼都不會動到。&lt;/p&gt;

&lt;p&gt;這種模式讓串流加密演算法得以和區塊加密器分離開來。在 &lt;code&gt;crypto/cipher&lt;/code&gt; 套件中的 &lt;code&gt;Block&lt;/code&gt; 介面定義了區塊加密器的行為：把一塊資料加密。只要再配合 &lt;em&gt;bufio&lt;/em&gt; 套件，實作這個介面的加密器就可以用來建構串流式的加密器 (同時也是 &lt;code&gt;Stream&lt;/code&gt; 介面)，而不用深入研究每個區塊加密器的實作方式。&lt;/p&gt;

&lt;p&gt;&lt;code&gt;crypto/cipher&lt;/code&gt; 介面看起來大致像這樣&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type Block interface {
    BlockSize() int
    Encrypt(src, dst []byte)
    Decrypt(src, dst []byte)
}

type Stream interface {
    XORKeyStream(dst, src []byte)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;而以下是 counter mode 的串流的定義，用來把區塊加密轉成串流加密。你可以注意到區塊加密的細節被隱藏起來了&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// NewCTR returns a Stream that encrypts/decrypts using the given Block in
// counter mode. The length of iv must be the same as the Block&#39;s block size.
func NewCTR(block Block, iv []byte) Stream
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這個函式不止可以用在特定某種區塊加密法，而是可以用任意區塊加密器建立不同的串流加密器。因為它回傳的是介面，所以只要換個建構子便能把加密方式換掉。&lt;/p&gt;

&lt;p&gt;譯註：本段需要對加密演算法有研究的人重新順一次，PR Plz！&lt;/p&gt;

&lt;h3 id=&#34;介面與方法:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;介面與方法&lt;/h3&gt;

&lt;p&gt;既然幾乎所有的東西都可以加上方法，那就表示幾乎所有東西都可以實作介面。一個很明顯的例子就是 &lt;code&gt;http&lt;/code&gt; 套件的 &lt;code&gt;Handler&lt;/code&gt; 介面。任何實做這個介面的東西都可以用來處理 HTTP 請求。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type Handler interface {
    ServeHTTP(ResponseWriter, *Request)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;code&gt;ResponseWriter&lt;/code&gt; 本身就是個介面，提供一些方法讓你可以回應 HTTP 請求；這包括了標準的 &lt;code&gt;Write&lt;/code&gt; 方法，所以它也可以用在任何需要 &lt;code&gt;io.Writer&lt;/code&gt; 的地方。而 &lt;code&gt;Request&lt;/code&gt; 則是把 HTTP 請求轉成一個方便你存取的 struct。&lt;/p&gt;

&lt;p&gt;為了便於說明，讓我們把 HTTP 簡化成只有 GET 沒有 POST，這樣的簡化不會影響我們設定 HTTP 請求的處理程式。以下是個很直觀但是也很完整的處理程式，用來記錄有多少人次來看過這一頁。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// Simple counter server.
type Counter struct {
    n int
}

func (ctr *Counter) ServeHTTP(w http.ResponseWriter, req *http.Request) {
    ctr.n++
    fmt.Fprintf(w, &amp;quot;counter = %d\n&amp;quot;, ctr.n)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;(思考一下，為什麼 &lt;code&gt;Fprintf&lt;/code&gt; 可以變成 HTTP 回應呢？) 做為參考，這是把這個處理程式綁定在某個網址的方法。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;import &amp;quot;net/http&amp;quot;
...
ctr := new(Counter)
http.Handle(&amp;quot;/counter&amp;quot;, ctr)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;但為什麼要用 struct 呢？用整數就夠了！ (接收器要指標型別，這樣才不會只是改到副本)&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// Simpler counter server.
type Counter int

func (ctr *Counter) ServeHTTP(w http.ResponseWriter, req *http.Request) {
    *ctr++
    fmt.Fprintf(w, &amp;quot;counter = %d\n&amp;quot;, *ctr)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;那如果你的程式有一些內部狀態，要在訪客拜訪這個頁面的時候處理呢？那就用 channel 吧&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// A channel that sends a notification on each visit.
// (Probably want the channel to be buffered.)
type Chan chan *http.Request

func (ch Chan) ServeHTTP(w http.ResponseWriter, req *http.Request) {
    ch &amp;lt;- req
    fmt.Fprint(w, &amp;quot;notification sent&amp;quot;)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;最後，假設我們要在 &lt;code&gt;/args&lt;/code&gt; 這個頁面裡顯示我們用來啟動伺服器的命令列參數。只是要輸出命令列參數的話很簡單：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func ArgServer() {
    fmt.Println(os.Args)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;但是要怎樣讓它變成 HTTP 伺服器呢？我們當然可以把 &lt;code&gt;ArgServer&lt;/code&gt; 變成某種自訂型別的方法，這樣只要忽略那個型別的值就好了。但還有更優雅的解決方式。既然除了指標和介面之外的東西都可以加上方法，那我們當然也可以為函式加上方法。在 &lt;code&gt;http&lt;/code&gt; 套件中有這樣的一段程式碼：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// The HandlerFunc type is an adapter to allow the use of
// ordinary functions as HTTP handlers.  If f is a function
// with the appropriate signature, HandlerFunc(f) is a
// Handler object that calls f.
type HandlerFunc func(ResponseWriter, *Request)

// ServeHTTP calls f(c, req).
func (f HandlerFunc) ServeHTTP(w ResponseWriter, req *Request) {
    f(w, req)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;code&gt;HandlerFunc&lt;/code&gt; 是一個擁有 &lt;code&gt;ServeHTTP&lt;/code&gt; 方法的型別，所以這個型別當然可以處理 HTTP 請求。注意看實作：接收器接收的是一個函式 &lt;code&gt;f&lt;/code&gt;，然後去呼叫這個函式。看起來可能有點奇怪，但其實這跟上面那個接收 channel的例子沒有什麼不同。&lt;/p&gt;

&lt;p&gt;所以要把 &lt;code&gt;ArgServer&lt;/code&gt; 變成 HTTP 伺服器，首先我們要把它的定義改成正確的樣式&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// Argument server.
func ArgServer(w http.ResponseWriter, req *http.Request) {
    fmt.Fprintln(w, os.Args)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;現在 &lt;code&gt;ArgServer&lt;/code&gt; 的定義跟 &lt;code&gt;HandlerFunc&lt;/code&gt; 的定義是相容的了，這表示可以把它強制轉型成 &lt;code&gt;HandlerFunc&lt;/code&gt; 型別好存取 &lt;code&gt;ServeHTTP&lt;/code&gt; 方法，就像我們之前把 &lt;code&gt;Sequence&lt;/code&gt; 轉成 &lt;code&gt;IntSlice&lt;/code&gt; 一樣。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;http.Handle(&amp;quot;/args&amp;quot;, http.HandlerFunc(ArgServer))
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;當有人拜訪這個網址的時候，處理這個網址的處理程式會是 &lt;code&gt;ArgServer&lt;/code&gt;，而它的型別會是 &lt;code&gt;HandlerFunc&lt;/code&gt;。因為型別是 &lt;code&gt;HandlerFunc&lt;/code&gt;，所以 HTTP 伺服器會執行它的 &lt;code&gt;ServeHTTP&lt;/code&gt; 方法，而在 &lt;code&gt;ServeHTTP&lt;/code&gt; 裡又會呼叫 &lt;code&gt;ArgServer&lt;/code&gt; 本身，所以命令列參數就輸出到 HTTP 回應裡了。&lt;/p&gt;

&lt;p&gt;在這節裡，我們用了 struct、整數、channel 和函式來當 HTTP 伺服器。之所以能這樣用，是因為介面只是定義一組方法，而方法幾乎可以定義在任何東西上。&lt;/p&gt;

&lt;h2 id=&#34;空白識別子:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;空白識別子&lt;/h2&gt;

&lt;p&gt;我們之前已經在 &lt;em&gt;for range&lt;/em&gt; 迴圈和 &lt;em&gt;map&lt;/em&gt; 的章節提過這個名詞好幾次。你可以指定任意型別的任何值給空白識別子，這個值會被忽略。這有點像是 unix 的 */dev/null*：只能寫入，通常是用在你需要有一個變數，但它的值一點都不重要的時候。之前我們有看過幾次這種用法了。&lt;/p&gt;

&lt;h3 id=&#34;空白識別子與多重指定:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;空白識別子與多重指定&lt;/h3&gt;

&lt;p&gt;&lt;em&gt;for range&lt;/em&gt; 迴圈那節的例子，就是空白識別子在多重指定裡的應用。&lt;/p&gt;

&lt;p&gt;如果在等號的左邊需要好幾個變數，但其中一個不會被用到，你就可以用空白識別子取代它，這樣可以避免配置多餘的變數，也可以清楚顯示這個值會被忽略。比如我們呼叫一個會回傳某值和錯誤碼的函式，但我們只需要錯誤碼：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;if _, err := os.Stat(path); os.IsNotExist(err) {
	fmt.Printf(&amp;quot;%s does not exist\n&amp;quot;, path)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;有時候你會看到有人用空白識別子忽略錯誤碼，這是非常糟的錯誤示範。務必要檢察錯誤碼，每個錯誤碼的存在都是有理由的。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// Bad! This code will crash if path does not exist.
fi, _ := os.Stat(path)
if fi.IsDir() {
    fmt.Printf(&amp;quot;%s is a directory\n&amp;quot;, path)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;沒有用到的引用和變數:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;沒有用到的引用和變數&lt;/h3&gt;

&lt;p&gt;如果定義了一個變數，或是引入了一個套件，卻沒有真的用到它們，那麼就會發生編譯錯誤。引入用不到的套件會讓程式變肥、編譯變慢，而定義用不到的變數至少會浪廢記憶體，也會拖慢效能，甚至可能是某種 bug 的跡象。當程式正在密集開發的時候，這種情況會比較常見。為了要讓編譯成功，你得一直來來回回的加上、刪除相關的引用和定義，實在很擾人。空白識別子提供了一個暫時的解決方案。&lt;/p&gt;

&lt;p&gt;以下這個寫到一半的程式引用了兩個目前沒有用到的套件 &lt;code&gt;fmt&lt;/code&gt; 和 &lt;code&gt;os&lt;/code&gt;，以及一個沒有用到的變數 &lt;code&gt;fd&lt;/code&gt;，所以編譯不會過，但如果有什麼辦法可以知道程式現在有沒有錯誤的話就好了。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;package main

import (
    &amp;quot;fmt&amp;quot;
    &amp;quot;io&amp;quot;
    &amp;quot;log&amp;quot;
    &amp;quot;os&amp;quot;
)

func main() {
    fd, err := os.Open(&amp;quot;test.go&amp;quot;)
    if err != nil {
        log.Fatal(err)
    }
    // TODO: use fd.
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如果想暫時跳過引用問題，可以把套件中的任意識別子指定給空白識別子。同樣地，你也可以透過把變數指定給空白識別子的方式來避開變數使用的問題。以下的程式就可以成功編譯了：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;package main

import (
    &amp;quot;fmt&amp;quot;
    &amp;quot;io&amp;quot;
    &amp;quot;log&amp;quot;
    &amp;quot;os&amp;quot;
)

var _ = fmt.Printf // For debugging; delete when done.
var _ io.Reader    // For debugging; delete when done.

func main() {
    fd, err := os.Open(&amp;quot;test.go&amp;quot;)
    if err != nil {
        log.Fatal(err)
    }
    // TODO: use fd.
    _ = fd
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;慣例上，用來避開引用錯誤的這兩行宣告，要放在引用的正下方，也要加上註解。這是為了未來可以回想起程式中有這樣的情況。&lt;/p&gt;

&lt;h3 id=&#34;為了副作用而引用:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;為了副作用而引用&lt;/h3&gt;

&lt;p&gt;像上一個例子中 &lt;code&gt;fmt&lt;/code&gt; 和 &lt;code&gt;os&lt;/code&gt; 的引用，要嘛補上使用它們的程式碼，要嘛就該刪除掉，空白識別子只是標識出我們還有相關工作要作。不過有時我們引用套件只是為了它的副作用，套件本身我們不會用到。舉例來說，&lt;code&gt;net/http/pprof&lt;/code&gt; 套件在它的 &lt;code&gt;init&lt;/code&gt; 函式中會註冊一個 HTTP 處理程式，用來提供相關的調校資訊。它有提供 API，不過通常你並不會用到，只會從 web 頁面上存取它。如果只需要一個套件的副作用，你可以用空白識別子作它的別名&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;import _ &amp;quot;net/http/pprof&amp;quot;
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這樣子你就沒有辦法在程式中使用這個套件中任何的識別子：在目前的源碼檔中，這個套件沒有名字。這表明了我們是為了它的副作用才引用的：如果你給了它名字而又沒有用到它，那就會有編譯錯誤。&lt;/p&gt;

&lt;h3 id=&#34;檢查型別:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;檢查型別&lt;/h3&gt;

&lt;p&gt;如同我們在 &lt;em&gt;介面&lt;/em&gt; 章節中提到的，型別不需要定義它實作了些什麼介面，只要實作那個介面定義的所有方法就夠了。實作中，大部份的介面型別轉換都是靜態的，也就是在編譯時完成的。比如你傳一個 &lt;code&gt;*os.File&lt;/code&gt; 型別的變數給需要 &lt;code&gt;io.Reader&lt;/code&gt; 的函式當參數，除非 &lt;code&gt;*os.File&lt;/code&gt; 有實作 &lt;code&gt;io.Reader&lt;/code&gt; 定義的所有方法，否則編譯的時候就會出錯了。&lt;/p&gt;

&lt;p&gt;也是有些型別檢查是執行時才做的，像 &lt;code&gt;encoding/json&lt;/code&gt; 套件中的 &lt;code&gt;Marshal&lt;/code&gt; 函式就是一個例子。當 JSON 編碼器收到一個有實作編碼介面的值，編碼器就會呼叫介面中定義的編碼方法；否則會用預設的方式來編碼。編碼器會在執行時才用型別斷言來確認型別：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;m, ok := val.(json.Marshaler)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;像是在檢查的時候，其實根本不會用到 &lt;code&gt;m&lt;/code&gt;，我們只關心 &lt;code&gt;ok&lt;/code&gt; 還是不ok，這時候就是空白識別子的出場時機：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;if _, ok := val.(json.Marshaler); ok {
    fmt.Printf(&amp;quot;value %v of type %T implements json.Marshaler\n&amp;quot;, val, val)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;當你想保證某變數確實有實作某介面的時候，可以用型別轉換來處理。例如有個型別，假設是 &lt;code&gt;json.RawMessage&lt;/code&gt; 好了，它需要自訂的 JSON 格式，它就應該要實作 &lt;code&gt;json.Marshaler&lt;/code&gt; 介面，但編譯器沒辦法幫你自動檢查 (譯註：因為 &lt;code&gt;json.Marshal&lt;/code&gt; 並不限制參數一定要實作這個介面)。 如果它沒有實作這個介面， JSON 編碼器還是可以把它轉成 JSON，但顯然結果不會是我們想要的。如果要保證我們的實作沒有問題，可以用空白識別子作全域的定義：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;var _ json.Marshaler = (*RawMessage)(nil)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這個定義把 &lt;code&gt;*RawMessage&lt;/code&gt; 指定給某個 &lt;code&gt;json.Mashaler&lt;/code&gt; 型別的變數 (空白識別子)，這會強制把 &lt;code&gt;*RawMessage&lt;/code&gt; 轉型成 &lt;code&gt;json.Marshaler&lt;/code&gt;，所以如果 &lt;code&gt;*RawMessage&lt;/code&gt; 沒有實作 &lt;code&gt;json.Marshaler&lt;/code&gt; 的話，編譯就會出錯，我們也就知道事情不對勁了。&lt;/p&gt;

&lt;p&gt;這種寫法指明了我們只是想做型別檢查，而不是要定義一個新的變數。不過也不要看到黑影就開槍，真的不需要把每個型別都加上這種檢查。慣例上我們只在編譯器沒辦法為我們檢查的時候才會用這招，不過這也是很少見的事。&lt;/p&gt;

&lt;h2 id=&#34;嵌入:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;嵌入&lt;/h2&gt;

&lt;p&gt;Go 沒有提供常見的、基於型別的繼承，不過確實可以像繼承那樣，借用先人的智慧來完成工作：把某個型別嵌入到 struct 或介面中。&lt;/p&gt;

&lt;p&gt;嵌入介面很簡單，我們之前提過了 &lt;code&gt;io.Reader&lt;/code&gt; 和 &lt;code&gt;io.Writer&lt;/code&gt; 兩個介面：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type Reader interface {
    Read(p []byte) (n int, err error)
}

type Writer interface {
    Write(p []byte) (n int, err error)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;code&gt;io&lt;/code&gt; 套件中還定義了許多其他介面，讓你的型別可以實作這兩個方法。比如 &lt;code&gt;io.ReadWriter&lt;/code&gt;，它就只是把 &lt;code&gt;io.Reader&lt;/code&gt; 跟 &lt;code&gt;io.Writer&lt;/code&gt; 合併成一個介面。我們當然可以把這兩個方法的定義複製貼上，好定義新的介面。不過比較理想的方法是直接把它們嵌進去。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// ReadWriter is the interface that combines the Reader and Writer interfaces.
type ReadWriter interface {
    Reader
    Writer
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這很好懂： &lt;em&gt;ReadWriter&lt;/em&gt; 既可以當成是 *Reader*，也可以當成是 *Writer*。它是這兩個介面的聯集 (當然這兩個介面必須定義不同的方法)。你只能在介面中嵌入介面。&lt;/p&gt;

&lt;p&gt;類似的狀況也可以套用在 struct 的嵌入上，但 struct 的嵌入還有更多值得注意的部份。&lt;code&gt;bufio&lt;/code&gt; 套件裡有兩個 struct 型別：&lt;code&gt;bufio.Reader&lt;/code&gt; 和 &lt;code&gt;bufio.Writer&lt;/code&gt;。如同你所猜測的，它們實作了 &lt;code&gt;io&lt;/code&gt; 套件中的同名介面。而 &lt;code&gt;bufio&lt;/code&gt; 也有個型別會實作 *ReadWriter*：把 &lt;code&gt;Reader&lt;/code&gt; 跟 &lt;code&gt;Writer&lt;/code&gt; 嵌進來。它列出了這兩個型別，但沒有給它們屬性名稱。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// ReadWriter stores pointers to a Reader and a Writer.
// It implements io.ReadWriter.
type ReadWriter struct {
    *Reader  // *bufio.Reader
    *Writer  // *bufio.Writer
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;由於嵌進來的是指標，所以得先初始化之後才能用。如果你把 &lt;code&gt;ReadWriter&lt;/code&gt; struct 寫成這樣：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type ReadWriter struct {
    reader *Reader
    writer *Writer
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;那麼相應的方法會綁定在各自的屬性上，你就得自己重寫 &lt;code&gt;Read&lt;/code&gt; 和 &lt;code&gt;Write&lt;/code&gt; 方法，把工作轉發給內層，才能符合 &lt;code&gt;io.ReadWriter&lt;/code&gt; 的要求：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func (rw *ReadWriter) Read(p []byte) (n int, err error) {
    return rw.reader.Read(p)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;透過嵌入，我們就可以避免這樣的複製貼上問題。你將可以直接存取內嵌的型別所定義的方法，這代表我們一次符合了 &lt;code&gt;io.Reader&lt;/code&gt;, &lt;code&gt;io.Writer&lt;/code&gt; 和 &lt;code&gt;io.ReadWriter&lt;/code&gt; 的定義。&lt;/p&gt;

&lt;p&gt;嵌入跟繼承有個很重要的差異。雖然我們可以從外層的型別去呼叫內層定義的方法，但方法的接收器所接收到的會是內層的型別。當我們呼叫 &lt;code&gt;bufio.ReadWriter&lt;/code&gt; 的 &lt;code&gt;Read&lt;/code&gt; 方法時，它的情況會跟上上個例子中轉發的動作一樣：&lt;code&gt;Read&lt;/code&gt; 接收到的不會是 &lt;code&gt;ReadWriter&lt;/code&gt; 實體，而是 &lt;code&gt;ReadWriter&lt;/code&gt; 實體內部的某個不具名的 &lt;code&gt;*Reader&lt;/code&gt; 實體。&lt;/p&gt;

&lt;p&gt;你也可以單純為了方便而使用嵌入。下面示範了一個嵌入的屬性，和一個正常的屬性。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type Job struct {
    Command string
    *log.Logger
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;code&gt;Job&lt;/code&gt; 型別現在也有 &lt;code&gt;Log&lt;/code&gt;, &lt;code&gt;Logf&lt;/code&gt; 和其他 &lt;code&gt;*log.Logger&lt;/code&gt; 定義的方法了。當然我們也可以給它一個屬性名稱，不過顯然沒有這個必要。現在只要初始化完之後，我們就可以&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;job.Log(&amp;quot;starting now...&amp;quot;)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;code&gt;Logger&lt;/code&gt; 也是 &lt;code&gt;Job&lt;/code&gt; 裡的一個正常屬性，所以我們可以在建構子中初始化它&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func NewJob(command string, logger *log.Logger) *Job {
    return &amp;amp;Job{command, logger}
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;或是用複合結構表達式&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;job := &amp;amp;Job{command, log.New(os.Stderr, &amp;quot;Job: &amp;quot;, log.Ldate)}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如果我們要直接存取內嵌的型別，那麼它的型別 (忽略套件的部份) 就是它的屬性名稱，就像上面例子中的 &lt;code&gt;ReadWriter&lt;/code&gt; 一樣。這個例子裡，如果我們要某個 &lt;code&gt;Job&lt;/code&gt; 型別的變數 &lt;code&gt;job&lt;/code&gt; 裡的 &lt;code&gt;log.Logger&lt;/code&gt;，那應該要用 &lt;code&gt;job.Logger&lt;/code&gt;。當需要覆寫它的方法時會很有用：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func (job *Job) Logf(format string, args ...interface{}) {
    job.Logger.Logf(&amp;quot;%q: %s&amp;quot;, job.Command, fmt.Sprintf(format, args...))
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;嵌入機制有造成命名衝突的可能，但解決的規則也很簡單。首先，外層的屬性和方法會蓋掉內層的屬性和方法。如果 &lt;code&gt;log.Logger&lt;/code&gt; 有一個屬性也叫 &lt;code&gt;Command&lt;/code&gt;，那它就只能透過 &lt;code&gt;job.Logger.Command&lt;/code&gt; 的方式存取。&lt;/p&gt;

&lt;p&gt;其次，如果在同一層出現命名衝突，通常這會是個錯誤。如果 &lt;code&gt;Job&lt;/code&gt; 型別裡已經有 &lt;code&gt;Logger&lt;/code&gt; 屬性的情況下嵌了 &lt;code&gt;log.Logger&lt;/code&gt; 進去，這八成是哪裡出了問題。然而，如果這衝突的命名只出現在定義的時候，外部程式不會存取到的話就沒問題。這是為了保護內層的型別不會被外部修改：顯然不會被外部使用的那些資料就不需要這種保護。&lt;/p&gt;

&lt;h2 id=&#34;並行運算:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;並行運算&lt;/h2&gt;

&lt;h3 id=&#34;以溝通來共享:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;以溝通來共享&lt;/h3&gt;

&lt;p&gt;並行運算是深奧的問題，這裡只討論一些 Go 語言中相關的重點。&lt;/p&gt;

&lt;p&gt;在許多環境中，我們很難實作正確的資料共享方式，這讓並行運算也跟著困難起來。Go 語言推行另一種模式來解決這個問題：把需要共享的資料透過 channel 來互相傳遞，而不是讓每個程序自行存取它。這樣一次只會有一個 goroutine 可以存取它，那麼在設計層面上就杜絕了資料存取競爭 (data race) 的問題。我們想了一句標語來幫你用這種方式思考：&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;不要透過共享的資料來互相溝通，要用互相溝通的方式來共享資料
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這種方式不需要無限上綱，比如參考計數模式最好還是用「把一個同步鎖加在整數上」的方式處理。不過作為一種比較高階的方法，用 channel 做存取控制可以讓你寫出比較簡單明瞭的程式。&lt;/p&gt;

&lt;p&gt;要理解為什麼，我們先假設現在有一個程式，跑在一台單核的電腦上。它絕對不會有同步問題，你知道，我知道，獨眼龍也知道。然後我們把這個程式再跑一份起來，顯然也不會有同步問題。現在，讓這兩份程式互相溝通。如果它們是用彼此溝通的那個管道來做同步控制，那它們還是不會有同步問題。Unix 的管道正是這種模式的完美詮釋。雖然 Go 對於並行運算的處理模式是從 Hoare 的 Communicating Sequential Processes (CSP) 發想的，但你也可以把它想成是比較一般化、不會有型別問題的 Unix 管道。&lt;/p&gt;

&lt;h3 id=&#34;goroutine:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;Goroutine&lt;/h3&gt;

&lt;p&gt;之所以會取這個名字，是因為現有的各種類似名稱：thread、副程序、程序…等等，都不能精準描述它。Goroutine 的模型很簡單：它是一個函式，和其他 goroutine 在同一個位址空間中做並行運算。它很小，只需要比配置堆疊再多一點的資源，而且它的堆疊一開始也會很小，所以它是真的很小。但當需要的時候，它也會靠在 heap 中配置空間來增長。&lt;/p&gt;

&lt;p&gt;所有的 Goroutine 會自動分配到數個作業系統層級的 thread 中執行，所以某個 goroutine 如果因為等待 I/O 動作而需要暫停的時候，其他 goroutine 還是會繼續執行。它的設計可以隱藏掉很多 thread 控制的細節。&lt;/p&gt;

&lt;p&gt;當你呼叫函式或是方法的時候，前面加上 &lt;code&gt;go&lt;/code&gt; 關鍵字，就可以把它扔進一個新的 goroutine 裡執行。函式結束的時候，goroutine 也會悄悄地跟著結束，效果很像在 Unix 的 shell 裡用 &lt;code&gt;&amp;amp;&lt;/code&gt; 把程式丟到背景執行。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;go list.Sort()  // run list.Sort concurrently; don&#39;t wait for it.
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;函式表達式也是很常配合 &lt;code&gt;go&lt;/code&gt; 使用的&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func Announce(message string, delay time.Duration) {
    go func() {
        time.Sleep(delay)
        fmt.Println(message)
    }()  // Note the parentheses - must call the function.
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;Go 語言的函式表達式就是 closure：被函式參考到的變數，在函式結束前是不會釋放的。&lt;/p&gt;

&lt;p&gt;這些範例沒什麼實用價值，因為你不知道它何時才會結束。要做到這點，就得靠 channel 了。&lt;/p&gt;

&lt;h3 id=&#34;channel:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;Channel&lt;/h3&gt;

&lt;p&gt;像 &lt;code&gt;map&lt;/code&gt; 一樣，channel 要用 &lt;code&gt;make&lt;/code&gt; 配置，結果會是一個指標，指向某種內部的資料結構。如果你加上一個整數當參數，它會是 channel 的緩衝區的大小。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;ci := make(chan int)            // unbuffered channel of integers
cj := make(chan int, 0)         // unbuffered channel of integers
cs := make(chan *os.File, 100)  // buffered channel of pointers to Files
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;若是用無緩衝區的 channel 來溝通 - 就是用它來同步交換資料 - 可以保證兩端的 goroutine 的狀態都會是確定的。&lt;/p&gt;

&lt;p&gt;我們有很多使用 channel 的慣例。上一節我們把一個排序的動作扔到了背景，我們可以用 channel 來讓外部程式等待排序完畢。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;c := make(chan int)  // Allocate a channel.
// Start the sort in a goroutine; when it completes, signal on the channel.
go func() {
    list.Sort()
    c &amp;lt;- 1  // Send a signal; value does not matter.
}()
doSomethingForAWhile()
&amp;lt;-c   // Wait for sort to finish; discard sent value.
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;接收端會暫停到有資料可以接收為止。如果 channel 沒有緩衝區的話，發送端也會暫停到有人來接收為止。如果有緩衝區的話，那就會暫停到資料放進緩衝區為止：如果緩衝區滿了，就要等到有人來接收資料，讓緩衝區騰出空間來，資料才能放進緩衝區。&lt;/p&gt;

&lt;p&gt;有緩衝區的 channel 有點像是同步旗標，比如可以用來限制流量。下個範例中，連入的請求會傳給 &lt;code&gt;handle&lt;/code&gt;，而它會傳送一個值給 channel，處理請求，然後再從 channel 中讀取一個值以表示「我處理好了，可以換下一位了」。channel 緩衝區的大小決定了同時可以處理幾個請求。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;var sem = make(chan int, MaxOutstanding)

func handle(r *Request) {
    sem &amp;lt;- 1    // Wait for active queue to drain.
    process(r)  // May take a long time.
    &amp;lt;-sem       // Done; enable next request to run.
}

func Serve(queue chan *Request) {
    for {
        req := &amp;lt;-queue
        go handle(req)  // Don&#39;t wait for handle to finish.
    }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;一旦有 &lt;code&gt;MaxOutStanding&lt;/code&gt; 個請求同時在處理中，後來連入的請求就會在傳送資料給 channel 的時候被強制暫停，直到有前面的請求處理完為止。&lt;/p&gt;

&lt;p&gt;但這個設計有個問題：&lt;code&gt;Serve&lt;/code&gt; 會為每個連入的請求建立一個 goroutine，就算現在 channel 滿了也一樣。所以如果連入的速度太快，那 goroutine 就會越來越多，直到系統資源耗盡當機。我們可以改寫 &lt;code&gt;Serve&lt;/code&gt; 來管制 goroutine 的產生。以下的範例很直觀，但我們留了一個 bug 之後再修：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func Serve(queue chan *Request) {
    for req := range queue {
        sem &amp;lt;- 1
        go func() {
            process(req) // Buggy; see explanation below.
            &amp;lt;-sem
        }()
    }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;在 &lt;em&gt;for&lt;/em&gt; 迴圈上定義的變數是會重覆使用的，這讓上一個範例產生了一個 bug：所有的 goroutine 共享了同一個 &lt;code&gt;req&lt;/code&gt; 變數。這顯然不是我們想要的，所以我們得確定每個 goroutine 取得的 &lt;code&gt;req&lt;/code&gt; 都獨一無二。其中一個方式是把它用參數傳進去。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func Serve(queue chan *Request) {
    for req := range queue {
        sem &amp;lt;- 1
        go func(req *Request) {
            process(req)
            &amp;lt;-sem
        }(req)
    }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;你可以上下比對一下，幫助你理解 closure 是怎麼運作的。另一種方式是乾脆定義一個同名的新變數給它：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func Serve(queue chan *Request) {
    for req := range queue {
        req := req // Create new instance of req for the goroutine.
        sem &amp;lt;- 1
        go func() {
            process(req)
            &amp;lt;-sem
        }()
    }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;也許 &lt;code&gt;req := req&lt;/code&gt; 看起來很詭異，但這在 Go 是合乎慣例的。你配置了一個全新的區域變數，使用跟外部變數相同的名稱，避開了迴圈變數的影響，也讓每個 goroutine 都有自己獨立的 &lt;code&gt;req&lt;/code&gt;。&lt;/p&gt;

&lt;p&gt;回到原本伺服器的例子，另一種資源管理的方式是一開始就啟動固定數量的 &lt;code&gt;handle&lt;/code&gt;，而每一個 &lt;code&gt;handle&lt;/code&gt; 都會從 channel 裡讀取連入的連線。而 &lt;code&gt;Serve&lt;/code&gt; 也會從另一個 channel 讀取是否該結束，所以啟動所有的 &lt;code&gt;handle&lt;/code&gt; 之後它就停在那等下班了。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func handle(queue chan *Request) {
    for r := range queue {
        process(r)
    }
}

func Serve(clientRequests chan *Request, quit chan bool) {
    // Start handlers
    for i := 0; i &amp;lt; MaxOutstanding; i++ {
        go handle(clientRequests)
    }
    &amp;lt;-quit  // Wait to be told to exit.
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;傳送-channel-的-channel:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;傳送 channel 的 channel&lt;/h3&gt;

&lt;p&gt;Go 語言中一個極重要的特性是：channel 是 first-class value，你可以任意的配置、傳遞它。常見的用法是用來實作安全的平行處理環境。&lt;/p&gt;

&lt;p&gt;在上一節的範例中，&lt;code&gt;handle&lt;/code&gt; 是一個理想狀態的處理程式，但我們沒有定義它是處理什麼型別的資料。如果它處理的型別裡，包含了一個 channel 可以用來回傳處理結果，那麼每個客戶端都可以提供它們所需要的回傳方式。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type Request struct {
    args        []int
    f           func([]int) int
    resultChan  chan int
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;客戶端會提供一個函式、函式所需的參數，以及一個用來回傳結果的 channel。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func sum(a []int) (s int) {
    for _, v := range a {
        s += v
    }
    return
}

request := &amp;amp;Request{[]int{3, 4, 5}, sum, make(chan int)}
// Send request
clientRequests &amp;lt;- request
// Wait for response.
fmt.Printf(&amp;quot;answer: %d\n&amp;quot;, &amp;lt;-request.resultChan)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;在伺服器端，我們只需要修改 &lt;code&gt;handle&lt;/code&gt; 函式就好&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func handle(queue chan *Request) {
    for req := range queue {
        req.resultChan &amp;lt;- req.f(req.args)
    }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;雖然這個範例還需要很多修改才能符合現實狀況，但這段程式已經是一個提供流量控管、平行處理的非阻斷式 RPC 系統，而且裡面沒有半個同步鎖。&lt;/p&gt;

&lt;h3 id=&#34;平行處理:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;平行處理&lt;/h3&gt;

&lt;p&gt;另外一種應用是我們可以在多個 CPU 核心間進行平行處理。如果計算可以分成好幾個彼此獨立的部份，那就可以同時進行，只要用一個 channel 來確認哪個部份已經執行完畢就好。&lt;/p&gt;

&lt;p&gt;假設我們要做一些很耗資源的向量運算，每個向量的運算彼此間互不關聯。像是下面這個理想狀態的例子&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type Vector []float64

// Apply the operation to v[i], v[i+1] ... up to v[n-1].
func (v Vector) DoSome(i, n int, u Vector, c chan int) {
    for ; i &amp;lt; n; i++ {
        v[i] += u.Op(v[i])
    }
    c &amp;lt;- 1    // signal that this piece is done
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;我們把每一個運算用迴圈一一啟動，每一個 CPU 核心處理一個。它們完成的順序並不固定，不過那不要緊，我們只要透過讀取 channel 來確定運算是否通通完成就好。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;const numCPU = 4 // number of CPU cores

func (v Vector) DoAll(u Vector) {
    c := make(chan int, numCPU)  // Buffering optional but sensible.
    for i := 0; i &amp;lt; numCPU; i++ {
        go v.DoSome(i*len(v)/numCPU, (i+1)*len(v)/numCPU, u, c)
    }
    // Drain the channel.
    for i := 0; i &amp;lt; numCPU; i++ {
        &amp;lt;-c    // wait for one task to complete
    }
    // All done.
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;而 CPU 數量可以透過 &lt;code&gt;runtime.NumCPU&lt;/code&gt; 取得&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;var numCPU = runtime.NumCPU()
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;另外還有一個函式 &lt;code&gt;runtime.GOMAXPROCS&lt;/code&gt;，它會回傳使用者定義的，一個 Go 程式最多可佔用的 CPU 核心數，預設值是 &lt;code&gt;runtime.NumCPU&lt;/code&gt; 的結果。你可以透過設定特定的環境變數，或是傳個正整數給它來做調整。如果我們決定使用使用者定義的值&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;var numCPU = runtime.GOMAXPROCS(0)
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;要注意，別把並行運算 (把程式結構調整成數個可以彼此獨立執行的組件) 和平行運算 (把一個運算分拆成幾個彼此獨立的小型運算，交給不同的 CPU 核心同時進行) 給搞混了。雖然 Go 語言適合並行運算的特性會讓你比較容易把問題用平行運算處理，但 Go 是適合並行運算的語言，不完全適合平行運算，所以某些平行運算的模式不適合用 Go。你可以看看&lt;a href=&#34;http://blog.golang.org/2013/01/concurrency-is-not-parallelism.html&#34;&gt;這個部落格裡的這份演講&lt;/a&gt;來深入了解兩者之間的差異。&lt;/p&gt;

&lt;h3 id=&#34;leaky-buffer:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;Leaky buffer&lt;/h3&gt;

&lt;p&gt;這些適合並行運算的工具還能讓非並行的運算變的更簡單明瞭。以下是個從 RPC 套件中抽象化之後的範例。客戶端不停地行某些來源取得資料，也許是網路。為了避免重複配置、釋放緩衝區，所以我們準備了一個閒置區，它是個 channel，裡面存放閒置的緩衝區。如果 channel 是空的，那我們就配置一個新的緩衝區。把資料放進緩衝區之後，我們就把整個緩衝區丟給伺服器做運算。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;var freeList = make(chan *Buffer, 100)
var serverChan = make(chan *Buffer)

func client() {
    for {
        var b *Buffer
        // Grab a buffer if available; allocate if not.
        select {
        case b = &amp;lt;-freeList:
            // Got one; nothing more to do.
        default:
            // None free, so allocate a new one.
            b = new(Buffer)
        }
        load(b)              // Read next message from the net.
        serverChan &amp;lt;- b      // Send to server.
    }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;伺服器則是不停地從客戶端接收緩衝區，處理裡面的資料，然後把用完的緩衝區放到閒置區。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func server() {
    for {
        b := &amp;lt;-serverChan    // Wait for work.
        process(b)
        // Reuse buffer if there&#39;s room.
        select {
        case freeList &amp;lt;- b:
            // Buffer on free list; nothing more to do.
        default:
            // Free list full, just carry on.
        }
    }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;客戶端從閒置區取得現成的緩衝區，拿不到的話就自己產生一個新的。伺服器把用完的緩衝區放回閒置區，如果閒置區滿了，就直接把這多餘的緩衝區丟棄，垃圾處理機制會負責釋放它。(如果&lt;code&gt;select&lt;/code&gt; 裡所有的 &lt;code&gt;case&lt;/code&gt; 部份都不成立，就會執行 &lt;code&gt;default&lt;/code&gt; 那部份，所以 &lt;code&gt;select&lt;/code&gt; 不會暫停) 這樣的實作只用短短幾行就作了一個 leaky buffer 式的閒置列表，靠的是有緩衝區的 channel 和垃圾處理機制。&lt;/p&gt;

&lt;h2 id=&#34;錯誤處理:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;錯誤處理&lt;/h2&gt;

&lt;p&gt;程式庫常常會回傳某種錯誤通知給呼叫者。如同前面提到的，多重回傳值讓你可以簡單的同時把正常的回傳值和錯誤狀況同時傳回去，你也應該使用這個方式回傳錯誤碼。舉例來說，&lt;code&gt;os.Open&lt;/code&gt; 發生錯誤的時候不只是回傳 &lt;code&gt;nil&lt;/code&gt;，它還同時會回傳一個錯誤碼。&lt;/p&gt;

&lt;p&gt;慣例上，錯誤碼會是內建的 &lt;code&gt;error&lt;/code&gt; 型別：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type error interface {
    Error() string
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;撰寫程式庫的人可以實作這個介面，並提供更完整、豐富的內容。如同前面所說，在回傳正常的值之外，&lt;code&gt;os.Open&lt;/code&gt; 還會回傳一個錯誤碼。如果一切正常，那錯誤碼就會是 &lt;code&gt;nil&lt;/code&gt;，而出問題的時候會是 &lt;code&gt;os.PathError&lt;/code&gt;。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// PathError records an error and the operation and
// file path that caused it.
type PathError struct {
    Op string    // &amp;quot;open&amp;quot;, &amp;quot;unlink&amp;quot;, etc.
    Path string  // The associated file.
    Err error    // Returned by the system call.
}

func (e *PathError) Error() string {
    return e.Op + &amp;quot; &amp;quot; + e.Path + &amp;quot;: &amp;quot; + e.Err.Error()
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;&lt;code&gt;PathError&lt;/code&gt; 的 &lt;code&gt;Error&lt;/code&gt; 方法會產生像這樣的字串&lt;/p&gt;

&lt;pre&gt;&lt;code&gt;open /etc/passwx: no such file or directory
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;像這樣在錯誤訊息中囊括了檔名、動作和實際獨發的系統錯誤，就算是顯示在八竿子打不著邊的遠處，也能看出來是出了什麼問題，因為它比單純顯示「找不到檔案」要更詳細。&lt;/p&gt;

&lt;p&gt;如果可以的話，錯誤訊息應該要能指明來源，比如前面加上套件名稱或是動作。比如在 &lt;code&gt;image&lt;/code&gt; 套件中，解碼錯誤的訊息是 &amp;ldquo;image: unknown format&amp;rdquo;。&lt;/p&gt;

&lt;p&gt;如果要針對不同的錯誤做處理的話，你可以用 type switch 或是型別斷言來取得詳細資料。比如說想要取出 &lt;code&gt;PathError&lt;/code&gt; 裡的 &lt;code&gt;Err&lt;/code&gt; 來做錯誤回復&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;for try := 0; try &amp;lt; 2; try++ {
    file, err = os.Create(filename)
    if err == nil {
        return
    }
    if e, ok := err.(*os.PathError); ok &amp;amp;&amp;amp; e.Err == syscall.ENOSPC {
        deleteTempFiles()  // Recover some space.
        continue
    }
    return
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;在第二個 &lt;code&gt;if&lt;/code&gt; 這裡我們用了型別斷言。如果型別不對，那 &lt;code&gt;ok&lt;/code&gt; 會是 &lt;code&gt;false&lt;/code&gt;，&lt;code&gt;e&lt;/code&gt; 也會是 &lt;code&gt;nil&lt;/code&gt;。如果型別正確，&lt;code&gt;ok&lt;/code&gt; 會是 &lt;code&gt;true&lt;/code&gt;，所以 錯誤碼和 &lt;code&gt;e&lt;/code&gt; 都會是 &lt;code&gt;*os.PathError&lt;/code&gt; 型別，那麼我們就可以取出並驗證相關資訊了。&lt;/p&gt;

&lt;h3 id=&#34;panic:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;Panic&lt;/h3&gt;

&lt;p&gt;通常我們會透過回傳錯誤值來回報錯誤，像是 &lt;code&gt;Read&lt;/code&gt; 就會回傳它讀取了幾 byte，以及錯誤碼。但如果錯誤是無法回復的呢？有時候程式會發生無論如何都要中斷的錯誤。&lt;/p&gt;

&lt;p&gt;因此，Go 語言中內建了 &lt;code&gt;panic&lt;/code&gt; 函式，效果是發出一個足以中斷程式的執行時期錯誤 (注意下一節)。這個函式接受一個任意型別的參數，通常是字串，輸出之後中斷程式。這也可以指出發生了某種不可能的情況，比如跳出了無窮迴圈。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// A toy implementation of cube root using Newton&#39;s method.
func CubeRoot(x float64) float64 {
    z := x/3   // Arbitrary initial value
    for i := 0; i &amp;lt; 1e6; i++ {
        prevz := z
        z -= (z*z*z-x) / (3*z*z)
        if veryClose(z, prevz) {
            return z
        }
    }
    // A million iterations has not converged; something is wrong.
    panic(fmt.Sprintf(&amp;quot;CubeRoot(%g) did not converge&amp;quot;, x))
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這只是範例，但程式庫應該避免使用 &lt;code&gt;panic&lt;/code&gt;。如果問題有任何方式可以解決或忽略，中斷程式肯定就是最差的做法。不過也是有反例：如果程式庫在初始化的時候發生錯誤，無法成功初始化，那使用 panic 中斷程式通常是合理的。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;var user = os.Getenv(&amp;quot;USER&amp;quot;)

func init() {
    if user == &amp;quot;&amp;quot; {
        panic(&amp;quot;no value for $USER&amp;quot;)
    }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;h3 id=&#34;recover:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;Recover&lt;/h3&gt;

&lt;p&gt;當你呼叫 &lt;code&gt;panic&lt;/code&gt; 的時候，包括執行時期的錯誤 (像是陣列索引超過範圍或是型別斷言失敗) 造成的 &lt;code&gt;panic&lt;/code&gt; 在內，它會循著現在的堆疊一路返回，執行所有 &lt;code&gt;defer&lt;/code&gt; 函式。如果堆疊沒有更多的返回資訊的話，程式就會結束。然而，你還是可以使用內建的 &lt;code&gt;recover&lt;/code&gt; 函式來重新取得控制，甚至是繼續正常的執行。&lt;/p&gt;

&lt;p&gt;&lt;code&gt;recover&lt;/code&gt; 會停止，不再繼續延著堆疊一路返回，同時回傳當初傳給 &lt;code&gt;panic&lt;/code&gt; 的參數。由於處理堆疊的時候只會執行 &lt;code&gt;defer&lt;/code&gt; 裡的程式，所以 &lt;code&gt;recover&lt;/code&gt; 要跟 &lt;code&gt;defer&lt;/code&gt; 一起用才有意義。&lt;/p&gt;

&lt;p&gt;&lt;code&gt;recover&lt;/code&gt; 的其中一種用法，是把伺服器的中，出錯的 goroutine 中斷，但不去影響其他的 goroutine。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-g&#34;&gt;func server(workChan &amp;lt;-chan *Work) {
    for work := range workChan {
        go safelyDo(work)
    }
}

func safelyDo(work *Work) {
    defer func() {
        if err := recover(); err != nil {
            log.Println(&amp;quot;work failed:&amp;quot;, err)
        }
    }()
    do(work)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;在這個範例中，如果 &lt;code&gt;do(work)&lt;/code&gt; 發生了 panic，結果會記錄下來，並且這個 goroutine 會結束執行，不會中斷其他的 goroutine。你不需要再 &lt;code&gt;defer&lt;/code&gt; 中加上什麼東西來完成這件事，一個 &lt;code&gt;recover&lt;/code&gt; 就夠了。&lt;/p&gt;

&lt;p&gt;除非是在 &lt;code&gt;defer&lt;/code&gt; 裡直接呼叫，不然 &lt;code&gt;recover&lt;/code&gt; 一律會回傳 &lt;code&gt;nil&lt;/code&gt;。所以你可以在 &lt;code&gt;defer&lt;/code&gt; 裡呼叫那些有使用到 &lt;code&gt;panic&lt;/code&gt; 和 &lt;code&gt;recover&lt;/code&gt; 的程式庫。舉例來說，&lt;code&gt;safelyDo&lt;/code&gt; 裡的 &lt;code&gt;defer&lt;/code&gt; 可以在 &lt;code&gt;recover&lt;/code&gt; 之前先呼叫 &lt;code&gt;log.Println&lt;/code&gt; 而不會受到目前 &lt;code&gt;panic&lt;/code&gt; 的狀態干擾。&lt;/p&gt;

&lt;p&gt;靠著這個模式，&lt;code&gt;do&lt;/code&gt; 函式 (和它所呼叫的其他程式庫) 可以用 &lt;code&gt;panic&lt;/code&gt; 來處理各種問題。在複雜程式中，我們可以用這種模式簡化錯誤處理。我們用一個理想化的 &lt;code&gt;regex&lt;/code&gt; 套件來作例子，它會透過 &lt;code&gt;panic&lt;/code&gt; 回報一個代表解析失敗的自訂錯誤型別。以下是這個錯誤型別 &lt;code&gt;Error&lt;/code&gt;, &lt;code&gt;error&lt;/code&gt; 方法 和 &lt;code&gt;Compile&lt;/code&gt; 函式的定義：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;// Error is the type of a parse error; it satisfies the error interface.
type Error string
func (e Error) Error() string {
    return string(e)
}

// error is a method of *Regexp that reports parsing errors by
// panicking with an Error.
func (regexp *Regexp) error(err string) {
    panic(Error(err))
}

// Compile returns a parsed representation of the regular expression.
func Compile(str string) (regexp *Regexp, err error) {
    regexp = new(Regexp)
    // doParse will panic if there is a parse error.
    defer func() {
        if e := recover(); e != nil {
            regexp = nil    // Clear return value.
            err = e.(Error) // Will re-panic if not a parse error.
        }
    }()
    return regexp.doParse(str), nil
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;如果 &lt;code&gt;doParse&lt;/code&gt; 發生錯誤，呼叫 &lt;code&gt;recover&lt;/code&gt; 的那段程式會把回傳值設成 &lt;code&gt;nil&lt;/code&gt;，因為 &lt;code&gt;defer&lt;/code&gt; 裡可以修改有預先命名的回傳值。而下一行會用型別斷言的方式檢查錯誤是不是我們自訂的那個型別。如果不是的話，型別斷言會失敗，產生一個新的執行時期錯誤，於是又會繼續循著堆疊返回，就像沒有呼叫過 &lt;code&gt;recover&lt;/code&gt; 那樣。這代表如果是一些意料外的錯誤，比如陣列索引超出範圍一類的，這種錯誤不會被我們捕捉到。&lt;/p&gt;

&lt;p&gt;&lt;code&gt;error&lt;/code&gt; 方法 (這不會跟內建的 &lt;code&gt;error&lt;/code&gt; 型別衝突，因為方法是綁定在某個型別裡面的) 可以讓你產生解析錯誤，又不用去花費精神思考堆疊處理順序的問題。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;if pos == 0 {
    re.error(&amp;quot;&#39;*&#39; illegal at start of expression&amp;quot;)
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;這個模式只應該在套件的內部使用。&lt;code&gt;Parse&lt;/code&gt; 把 &lt;code&gt;panic&lt;/code&gt; 轉成了錯誤碼，而非把它傳到套件之外。這是個該遵守的好原則。&lt;/p&gt;

&lt;p&gt;另外，這個慣例會改變真正的錯誤。然而，改變前後的錯誤都會列在錯誤回報中，所以問題的根源還是可以找得到。通常這樣已經很足夠，不論如何程式還是中斷了。但你若想要保留原本的錯誤類型，你可以再多寫幾行程式來過濾並重新發出原本的錯誤。這就當成作業留給讀者自己練習了。&lt;/p&gt;

&lt;h2 id=&#34;一個-web-伺服器:59460734ae8bfafc35f54a1a1aefb011&#34;&gt;一個 web 伺服器&lt;/h2&gt;

&lt;p&gt;讓我們用一個完整的 Go 程式來當作結束。這個程式其實比較像是在轉發其他的 web 服務。Google 提供了 &lt;a href=&#34;http://chart.apis.google.com&#34;&gt;http://chart.apis.google.com&lt;/a&gt; 這個把資料轉成圖表的服務。這個服務不太方便從瀏覽器中使用，因為你得把資料用 URL 參數傳過去。我們的程式提供了一個比較簡便的作法：提供一個表單，讓你透過表單把文字轉成二維條碼。你可以透過用手機掃描二維條碼來拜訪你輸入的網址。&lt;/p&gt;

&lt;p&gt;以下是完整的程式碼：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;package main

import (
    &amp;quot;flag&amp;quot;
    &amp;quot;html/template&amp;quot;
    &amp;quot;log&amp;quot;
    &amp;quot;net/http&amp;quot;
)

var addr = flag.String(&amp;quot;addr&amp;quot;, &amp;quot;:1718&amp;quot;, &amp;quot;http service address&amp;quot;) // Q=17, R=18

var templ = template.Must(template.New(&amp;quot;qr&amp;quot;).Parse(templateStr))

func main() {
    flag.Parse()
    http.Handle(&amp;quot;/&amp;quot;, http.HandlerFunc(QR))
    err := http.ListenAndServe(*addr, nil)
    if err != nil {
        log.Fatal(&amp;quot;ListenAndServe:&amp;quot;, err)
    }
}

func QR(w http.ResponseWriter, req *http.Request) {
    templ.Execute(w, req.FormValue(&amp;quot;s&amp;quot;))
}

const templateStr = `
&amp;lt;html&amp;gt;
&amp;lt;head&amp;gt;
&amp;lt;title&amp;gt;QR Link Generator&amp;lt;/title&amp;gt;
&amp;lt;/head&amp;gt;
&amp;lt;body&amp;gt;
{{if .}}
&amp;lt;img src=&amp;quot;http://chart.apis.google.com/chart?chs=300x300&amp;amp;cht=qr&amp;amp;choe=UTF-8&amp;amp;chl={{.}}&amp;quot; /&amp;gt;
&amp;lt;br&amp;gt;
{{.}}
&amp;lt;br&amp;gt;
&amp;lt;br&amp;gt;
{{end}}
&amp;lt;form action=&amp;quot;/&amp;quot; name=f method=&amp;quot;GET&amp;quot;&amp;gt;&amp;lt;input maxLength=1024 size=70
name=s value=&amp;quot;&amp;quot; title=&amp;quot;Text to QR Encode&amp;quot;&amp;gt;&amp;lt;input type=submit
value=&amp;quot;Show QR&amp;quot; name=qr&amp;gt;
&amp;lt;/form&amp;gt;
&amp;lt;/body&amp;gt;
&amp;lt;/html&amp;gt;
`
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;到 &lt;code&gt;main&lt;/code&gt; 函式之前的程式應該都算好懂。有一個命令列參數是設定我們的伺服器的 HTTP port。&lt;code&gt;templ&lt;/code&gt; 變數則是 HTML 樣版，我們稍後會解釋它。&lt;/p&gt;

&lt;p&gt;&lt;code&gt;main&lt;/code&gt; 函式解析命令列參數，然後用我們之前提到的技巧把 &lt;code&gt;QR&lt;/code&gt; 函式註冊成 HTTP 請求的處理程式。&lt;code&gt;http.ListenAndServe&lt;/code&gt; 會啟動 HTTP 伺服器，在伺服器運作期間主程式是暫停的。&lt;/p&gt;

&lt;p&gt;&lt;code&gt;QR&lt;/code&gt; 接收請求，然後把表單資料塞進樣版裡。&lt;/p&gt;

&lt;p&gt;&lt;code&gt;html/template&lt;/code&gt; 套件是一個強大的樣版引擎，而我們現在只用到一點皮毛。大致上，它會即時地根據你傳給 &lt;code&gt;templ.Execute&lt;/code&gt; 的參數把改寫 HTML 樣版。在樣版 (templaStr) 中，樣版引擎會依照雙重大括號包住的部份執行特定的動作。從 &lt;code&gt;{{if .}}&lt;/code&gt; 到 &lt;code&gt;{{end}}&lt;/code&gt; 這段代表如果目前的資料，又稱為 &lt;code&gt;.&lt;/code&gt;，不是空的，那就顯示或執行裡面的東西。也就是說如果目前資料是空的，這段就不會顯示出來。&lt;/p&gt;

&lt;p&gt;接下來的兩個 &lt;code&gt;{{.}}&lt;/code&gt; 會把 (表單傳來的) 資料顯示出來。樣版引擎會幫你處理好跳脫特殊字元的問題。&lt;/p&gt;

&lt;p&gt;剩下的是就單純的 HTML。如果覺得我們講解的太簡單，你可以參考 &lt;a href=&#34;http://golang.org/pkg/html/template/&#34;&gt;template 套件的說明文件&lt;/a&gt;。&lt;/p&gt;

&lt;p&gt;我們只用了幾行程式加上一些 HTML 碼，就做好了一個實用的小程式。Go 語言強大之處讓你可以用短短幾行程式就做到很多事情。&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>defer, Commit and Rollback</title>
      <link>https://ronmi.github.io/post/go/defer-commit-rollback/</link>
      <pubDate>Wed, 16 Sep 2015 00:00:00 +0000</pubDate>
      
      <guid>https://ronmi.github.io/post/go/defer-commit-rollback/</guid>
      <description>&lt;p&gt;使用 transaction 的時候，利用 defer 函式的特性 commit 或 rollback 比較合於 Go 的慣例。理論上應該有兩種形式，第一種是利用 &lt;code&gt;Commit()&lt;/code&gt; 之後 &lt;code&gt;Rollback()&lt;/code&gt; 不會真正執行的特性：&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;tx, err = db.Begin()
if err != nil {
	return err
}
defer tx.Rollback()

if _, err := tx.Exec(query); err != nil {
	return err
}

tx.Commit()
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;另外一種則是用 &lt;code&gt;panic&lt;/code&gt; 和 &lt;code&gt;recover&lt;/code&gt; 來確認在 defer 執行時的狀態是出錯還是正常&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;tx, err = db.Begin()
if err != nil {
	return
}
defer func() {
	if e := recover(); e != nil {
		tx.Rollback()
		err = e
	} else {
		tx.Commit()
	}
}

if _, err = tx.Exec(query); err != nil {
	panic(&amp;quot;Cannot execute sql query&amp;quot;)
}
return
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;後者看起來比較潮，但實際測速的結果&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func BenchmarkDeferRollback(b *testing.B) {
	fn := func(i int) {
		tx, err := db.Begin()
		if err != nil {
			b.Fatalf(&amp;quot;Cannot begin transaction: %s&amp;quot;, err)
		}
		defer tx.Rollback()
		q := tx.Stmt(newPadQuery)
		q.Exec(u.ID, fmt.Sprintf(&amp;quot;rollback%d&amp;quot;, i), &amp;quot;content&amp;quot;)
		tx.Commit()
	}
	b.ResetTimer()
	for i := 0; i &amp;lt; b.N; i++ {
		fn(i)
	}
}

func BenchmarkDeferIfElse(b *testing.B) {
	fn := func(i int) {
		tx, err := db.Begin()
		if err != nil {
			b.Fatalf(&amp;quot;Cannot begin transaction: %s&amp;quot;, err)
		}
		defer func () {
			if err := recover(); err != nil {
				tx.Rollback()
			} else {
				tx.Commit()
			}
		}()
		q := tx.Stmt(newPadQuery)
		q.Exec(u.ID, fmt.Sprintf(&amp;quot;if-else%d&amp;quot;, i), &amp;quot;content&amp;quot;)
	}
	b.ResetTimer()
	for i := 0; i &amp;lt; b.N; i++ {
		fn(i)
	}
}
&lt;/code&gt;&lt;/pre&gt;

&lt;pre&gt;&lt;code&gt;BenchmarkDeferRollback-8          100000             17449 ns/op
BenchmarkDeferIfElse-8            100000             17816 ns/op
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;我跑了大概十來次測試，都是直接 Rollback 比較快，雖然只有快幾 % 而已。猜測原因，可能是 panic 跟 recover 這兩個錯誤處理函式耗去了一些效能吧。&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>為 function 加上方法來測試 http handler</title>
      <link>https://ronmi.github.io/post/go/test-http-handler/</link>
      <pubDate>Wed, 16 Sep 2015 00:00:00 +0000</pubDate>
      
      <guid>https://ronmi.github.io/post/go/test-http-handler/</guid>
      <description>&lt;p&gt;在寫測試的時候，發現自己一直重複使用 &lt;code&gt;NewRequest&lt;/code&gt; 跟 &lt;code&gt;NewRecorder&lt;/code&gt;，所以想到可以在 function 上加上方法來協助處理這些問題。&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;type Test func(http.ResponseWriter, *http.Request)

func (f Test) Get(uri, cookie string) (rec *httptest.ResponseRecorder, err error) {
	rec = httptest.NewRecorder()
	req, err := http.NewRequest(&amp;quot;GET&amp;quot;, uri, nil)
	if err != nil {
		return
	}
	req.Header.Add(&amp;quot;Cookie&amp;quot;, cookie)
	f(rec, req)
	return
}
&lt;/code&gt;&lt;/pre&gt;

&lt;p&gt;使用起來像這樣&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-go&#34;&gt;func TestMyhandler(t *testing.T) {
	resp, err := Test(myhandler).Get(&amp;quot;/api/action&amp;quot;, &amp;quot;&amp;quot;)
	if err != nil {
		t.Fatalf(&amp;quot;Failed to create request for /api/action: %s&amp;quot;, err)
	}

	if resp.Body.String() != &amp;quot;success&amp;quot; {
		t.Errorf(&amp;quot;Request to /api/action failed: %s&amp;quot;, resp.Body.String())
	}
}
&lt;/code&gt;&lt;/pre&gt;
</description>
    </item>
    
  </channel>
</rss>